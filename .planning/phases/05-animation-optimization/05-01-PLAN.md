# Plan: Animation & Rendering Optimization (Phase 05)

## Goal
Optimize animations and rendering loops to ensure consistent 60fps performance and zero layout thrashing.

## TODO
- [x] **Step 1: Optimize Eye-Tracking (P5-T1)** <!-- id: 0 -->
    - [x] Refactor `ui/eye-tracker.js` to use `ResizeObserver` for caching eye container bounds.
    - [x] Update CSS variables on the nearest common ancestor (`.calculator-wrapper`) instead of `document.documentElement`.
- [x] **Step 2: Tune Display Rendering (P5-T2)** <!-- id: 1 -->
    - [x] Add dirty-check logic to `services/app.js`'s `fitDisplayText` to skip redundant calculations.
    - [x] Profile `ui/renderer.js` to ensure cache hits are prioritized.
- [x] **Step 3: Audit & Optimize CSS Transitions (P5-T3)** <!-- id: 2 -->
    - [x] Replace expensive property transitions (`width`, `max-width`, `margin`, `padding`) in `ui/styles.css` with `transform`-based equivalents where possible.
    - [x] Ensure all transitions use `will-change: transform` or `opacity`.
- [x] **Step 4: Performance Validation** <!-- id: 3 -->
    - [x] Run `tests/performance.spec.js` to verify hardware acceleration.
    - [x] Use Chrome DevTools to verify zero "Recalculate Style" or "Layout" events during eye tracking.

## Strategy
1. **Research & Context:** Already completed. I've identified layout thrashing in `eye-tracker.js` and expensive transitions in `styles.css`.
2. **Implementation:** I'll start by refactoring the eye-tracker, then the display rendering, and finally the CSS transitions.
3. **Verification:** I'll use the existing performance tests and manual profiling.

## Verification Plan
- **Automated:** `npx playwright test tests/performance.spec.js`
- **Manual:** Check for "Layout Thrashing" (purple bars) in the Performance tab while moving the mouse over the calculator.
- **Manual:** Verify `styles.css` transitions don't trigger "Paint" (green flashes) using the Rendering tab.
