# Plan: 09-04-PLAN.md — GPU-Side Animation Interpolation

**Phase:** 09 - High-Performance Batching & State Sync
**Focus:** Offloading UI transitions to the GPU.

## Goal
Implement vertex shader-based interpolation for UI transitions (position, size, opacity) to eliminate CPU overhead during layout shifts and animations.

## Requirements
- **REQ-WGL-06**: UI Animation Interpolation.

## Success Criteria
1.  **Interpolated Shaders**: Shaders accept start/end attributes and a start timestamp.
2.  **GPU-Side Math**: `mix()` and easing functions are applied in the vertex shader.
3.  **Smooth Transitions**: UI layout shifts are buttery smooth even under heavy CPU load.
4.  **Verifiability**: Performance tests show zero frame drops during layout-heavy transitions (e.g., sidebar opening).

## Technical Strategy
-   Expand instanced attributes to include `a_startRect`, `a_endRect`, `a_startTime`, and `a_duration`.
-   Use `u_time` from the GlobalState UBO as the reference.
-   Implement easing functions (smoothstep, quadratic) in GLSL.
-   Update `ui/renderer.js` to only send start/end states when a layout change is detected.

## Tasks
- [ ] Add transition-related attributes to `ui/webgl/shaders.js`. <!-- id: 0 -->
- [ ] Implement easing functions in GLSL. <!-- id: 1 -->
- [ ] Modify vertex shaders to perform `mix()` on position and opacity. <!-- id: 2 -->
- [ ] Update `ui/renderer.js` to track previous layout states for interpolation. <!-- id: 3 -->

## Verification Plan
- **Automated**: Run `tests/performance.spec.js` during layout shifts.
- **Manual**: Record a trace in Chrome DevTools to verify 60 FPS during sidebar opening.
