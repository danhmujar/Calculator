# 09-02-UAT.md — WebGL 2.0 UBO & Global Uniforms

**Phase:** 09 - High-Performance Batching & State Sync
**Plan:** 09-02
**Status:** PASSED

## Goal
Validate the implementation of Uniform Buffer Objects (UBOs) for centralized global shader state (resolution, time, DPR, scroll).

## Test Cases

### 1. UBO Infrastructure (Automated)
- **Action:** Run `npx playwright test tests/shader.spec.js`.
- **Expected:** `All shaders implement GlobalState UBO block` test passes.
- **Result:** **PASSED**
- **Evidence:**
  ```
  ✓  4 tests\shader.spec.js:59:5 › Shader Infrastructure › All shaders implement GlobalState UBO block (449ms)
  ```

### 2. Shader Compilation & Linkage (Automated)
- **Action:** Run `npx playwright test tests/shader.spec.js`.
- **Expected:** All programs (Primitive and Batch) compile successfully after prepending the UBO block.
- **Result:** **PASSED**
- **Evidence:**
  ```
  ✓  1 …\shader.spec.js:8:5 › Shader Infrastructure › ShaderManager compiles and links PRIMITIVE shaders successfully (1.1s)
  ```

### 3. Runtime Data Consistency (Integration)
- **Action:** Run `npx playwright test tests/renderer.spec.js`.
- **Expected:** BatchRenderer functions correctly and rendering is visible in Scientific/Standard modes.
- **Result:** **PASSED**
- **Evidence:**
  ```
  ✓  12 …sts\renderer.spec.js:146:5 › WebGL Rendering Engine › Scientific rows trigger WebGL rendering and highlights (2.4s)
  ✓  13 …r.spec.js:193:5 › WebGL Rendering Engine › BatchRenderer provides expected API (pushRect, pushGlyph, flush) (310ms)
  ```

### 4. Code Audit (Manual)
- **Action:** Inspect `ui/webgl/renderer.js` and `ui/webgl/shaders.js`.
- **Criteria:** 
    - [x] `std140` layout used for `GlobalState`.
    - [x] `u_resolution` removed from per-program uniform updates.
    - [x] `updateUBO` called once per frame in `render()`.
- **Result:** **PASSED**

## Summary
The UBO implementation correctly centralizes global shader state. Shaders are successfully using the shared buffer for `u_resolution`, `u_time`, and `u_dpr`. Redundant CPU-to-GPU uniform calls have been removed, meeting the performance optimization goal.

## Next Steps
Proceed to **09-03-PLAN.md: Multi-Draw Instancing & Batch Logic Refinement** to further optimize the primitive rendering pipeline.
