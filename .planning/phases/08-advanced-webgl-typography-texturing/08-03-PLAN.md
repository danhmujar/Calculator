---
phase: 08-advanced-webgl-typography-texturing
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified: [ui/webgl/shaders.js, ui/webgl/buffers.js, ui/webgl/renderer.js, tests/batch.spec.js, tests/renderer.spec.js]
autonomous: true
requirements: [REQ-WGL-05]
must_haves:
  truths:
    - "Multiple primitives and text glyphs are rendered in a single draw call"
    - "SDF text rendering maintains sharpness at various scales"
    - "Renderer uses Buffer Orphaning to avoid performance stalls"
  artifacts:
    - path: "ui/webgl/shaders.js"
      provides: "Unified Batch Shader (Rect + SDF Text)"
    - path: "ui/webgl/renderer.js"
      provides: "BatchRenderer class with instancing support"
    - path: "tests/batch.spec.js"
      provides: "Batch rendering unit tests"
  key_links:
    - from: "ui/webgl/renderer.js"
      to: "ui/webgl/atlas.js"
      via: "Texture sampling"
---

<objective>
Implement a high-performance Batch Renderer using WebGL 2.0 instancing. This renderer will combine both procedural UI primitives (rounded rects) and textured SDF glyphs into a unified rendering pipeline, minimizing draw calls and CPU-to-GPU overhead.

Purpose: 60 FPS performance for complex UI layouts.
Output: Updated renderer and shader files, new batch tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-advanced-webgl-typography-texturing/08-RESEARCH.md
@ui/webgl/shaders.js
@ui/webgl/buffers.js
@ui/webgl/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Batch Unit Test Scaffold</name>
  <files>tests/batch.spec.js</files>
  <action>
    Create a new Playwright test file `tests/batch.spec.js`.
    - Set up a test page with a WebGL 2.0 context.
    - Implement verification for instanced draw call counts using a mock or `gl.getParameter`.
    - Add tests for unified shader branching (rect vs text).
  </action>
  <verify>
    <automated>ls tests/batch.spec.js</automated>
  </verify>
  <done>Test scaffold exists and is ready to verify Task 1 and Task 2.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Unified Batch Shader</name>
  <files>ui/webgl/shaders.js</files>
  <action>
    Add `BATCH_VERT` and `BATCH_FRAG` to `ui/webgl/shaders.js`.
    - `BATCH_VERT`: Use `layout(location = 2..n)` for instanced attributes (pos, size, uv, color, type). Implement ortho projection from CSS pixels to clip space.
    - `BATCH_FRAG`: Branch between `sdRoundedBox` (for primitives) and `texture(u_atlas, v_uv)` (for SDF text).
    - Use `smoothstep` and `fwidth` for high-quality anti-aliasing.
  </action>
  <verify>
    <automated>npx playwright test tests/shader.spec.js</automated>
  </verify>
  <done>Unified shader compiles and supports both rendering modes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Instanced Buffer Management</name>
  <files>ui/webgl/buffers.js</files>
  <action>
    Extend `BufferManager` with `createInstancedVAO` and `updateInstanceBuffer`.
    - Use `gl.vertexAttribDivisor(index, 1)` for instanced attributes.
    - Implement a structured interleaved format for instance data (e.g., 8-10 floats per instance).
    - Ensure `updateInstanceBuffer` uses Buffer Orphaning (`gl.bufferData(..., null)` before update).
  </action>
  <verify>
    <automated>npx playwright test tests/batch.spec.js</automated>
  </verify>
  <done>VAO supports instanced drawing with dynamic updates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor Renderer to BatchRenderer & Update Tests</name>
  <files>ui/webgl/renderer.js, tests/renderer.spec.js</files>
  <behavior>
    - Multiple primitives added to the queue should be rendered in 1 draw call.
    - Primitives and Text Glyphs should be interleaved in the same buffer if possible.
  </behavior>
  <action>
    Implement a `BatchRenderer` class (or update `WebGLRenderer`) in `ui/webgl/renderer.js`.
    - Implement a command queue (`pushRect`, `pushGlyph`).
    - Implement a `flush()` method that updates the instance buffer and calls `gl.drawArraysInstanced`.
    - Integrate with the `TextureAtlas` created in plan 08-01.
    - Update `tests/renderer.spec.js` to account for the new BatchRenderer API.
  </action>
  <verify>
    <automated>npx playwright test tests/renderer.spec.js</automated>
  </verify>
  <done>Batch renderer is functional and performs optimally.</done>
</task>

</tasks>

<verification>
Manual verification: Use Spector.js to verify that a single draw call handles both the background rects and the math text in the scientific rows.
</verification>

<success_criteria>
1. Draw calls are reduced to under 5 per frame for the entire UI.
2. Text rendering is indistinguishable from DOM-based rendering.
3. No visual artifacts (jitter, clipping) are present.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-webgl-typography-texturing/08-03-SUMMARY.md`
</output>
