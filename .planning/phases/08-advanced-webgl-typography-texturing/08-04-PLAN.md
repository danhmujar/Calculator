---
phase: 08-advanced-webgl-typography-texturing
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified: [index.html, ui/uimanager.js, ui/webgl/renderer.js, tests/performance.spec.js]
autonomous: true
requirements: [REQ-VER-01]
must_haves:
  truths:
    - "User can toggle between DOM and WebGL rendering via a UI control"
    - "WebGL rendering is perfectly aligned with the original DOM layout"
    - "The application maintains 60 FPS during interactions and scrolling"
  artifacts:
    - path: "index.html"
      provides: "WebGL Toggle UI"
    - path: "ui/uimanager.js"
      provides: "Integration of WebGL layer and toggle logic"
  key_links:
    - from: "ui/uimanager.js"
      to: "ui/webgl/renderer.js"
      via: "Rendering cycle synchronization"
---

<objective>
Finalize the integration of the WebGL typography and batch rendering system. This plan focuses on wiring the new rendering capabilities into the main application loop, implementing a verification toggle for side-by-side comparison, and ensuring high-performance execution.

Purpose: Complete the Phase 8 migration and enable verification.
Output: Integrated application with WebGL toggle.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-advanced-webgl-typography-texturing/08-RESEARCH.md
@index.html
@ui/uimanager.js
@ui/webgl/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Verification Toggle UI</name>
  <files>index.html, ui/uimanager.js</files>
  <action>
    - Add a "WebGL" toggle checkbox to the header in `index.html`.
    - In `ui/uimanager.js`, listen for changes to this toggle.
    - When enabled: Show the WebGL canvas and hide/transparentize the corresponding DOM elements (especially the math text).
    - When disabled: Hide the WebGL canvas and restore DOM visibility.
    - Save the toggle state in `localStorage` for persistence across refreshes.
  </action>
  <verify>
    <automated>npx playwright test tests/display.spec.js</automated>
  </verify>
  <done>Toggle successfully switches between rendering modes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate BatchRenderer into UIManager Loop</name>
  <files>ui/uimanager.js, ui/webgl/renderer.js</files>
  <action>
    - Update `UIManager.updateDisplay` and `UIManager.addScientificRow` to coordinate with the `BatchRenderer`.
    - Ensure that every frame, the `BatchRenderer` is cleared, populated with the latest layout data (primitives + glyphs), and flushed.
    - Synchronize the WebGL canvas size and scroll offset with the scientific rows container.
    - Implement "Global Overlay Pattern" logic where the WebGL canvas covers the scientific area.
  </action>
  <verify>
    <automated>npx playwright test tests/renderer.spec.js</automated>
  </verify>
  <done>Scientific rows are fully rendered via WebGL when the toggle is active.</done>
</task>

<task type="auto">
  <name>Task 3: Performance Tuning and WebGL Text Benchmarks</name>
  <files>ui/webgl/renderer.js, tests/performance.spec.js</files>
  <action>
    - Perform a visual audit to ensure perfect alignment (pixel-perfect parity).
    - Adjust shader anti-aliasing (fwidth/smoothstep) for optimal clarity on various displays (Retina/High-DPI).
    - Update `tests/performance.spec.js` to include benchmarks for 100+ scientific rows specifically in WebGL mode.
    - Optimize the glyph extraction logic to only process visible rows (frustum culling at the CPU level).
  </action>
  <verify>
    <automated>npx playwright test tests/performance.spec.js</automated>
  </verify>
  <done>Application meets all performance and fidelity requirements, confirmed by automated benchmarks.</done>
</task>

</tasks>

<verification>
Manual verification: Open the scientific mode, add 10+ rows with complex math, and toggle the WebGL mode. There should be no visible shift or change in text quality, and the UI should remain responsive.
</verification>

<success_criteria>
1. Zero visual regressions from the original DOM UI.
2. Stable 60 FPS with heavy mathematical content.
3. Successful manual verification of the DOM-to-WebGL toggle.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-webgl-typography-texturing/08-04-SUMMARY.md`
</output>
