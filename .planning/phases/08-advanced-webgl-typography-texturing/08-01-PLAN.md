---
phase: 08-advanced-webgl-typography-texturing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ui/webgl/atlas.js, tests/atlas.spec.js]
autonomous: true
requirements: [REQ-WGL-04]
must_haves:
  truths:
    - "Dynamic glyphs are successfully rasterized to an offscreen canvas"
    - "SDF (Signed Distance Field) data is generated for each unique glyph/font combination"
    - "Texture atlas grows dynamically as new math symbols are encountered"
  artifacts:
    - path: "ui/webgl/atlas.js"
      provides: "Texture atlas management and SDF generation"
    - path: "tests/atlas.spec.js"
      provides: "Atlas unit tests"
  key_links:
    - from: "ui/webgl/atlas.js"
      to: "WebGL Texture"
      via: "gl.texSubImage2D"
---

<objective>
Implement a Dynamic Texture Atlas system that generates Signed Distance Fields (SDFs) for glyphs on-demand. This system will allow the WebGL layer to render arbitrary MathLive/KaTeX symbols with high fidelity and performance.

Purpose: Provide the textures needed for hardware-accelerated text rendering.
Output: `ui/webgl/atlas.js` utility and `tests/atlas.spec.js` unit test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-advanced-webgl-typography-texturing/08-RESEARCH.md
@ui/webgl/context.js
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Atlas Unit Test Scaffold</name>
  <files>tests/atlas.spec.js</files>
  <action>
    Create a new Playwright test file `tests/atlas.spec.js`.
    - Set up a test page with a WebGL 2.0 context.
    - Implement a basic verification for `TextureAtlas` API (yet to be implemented).
    - Add tests for SDF generation accuracy (checking pixel distances).
  </action>
  <verify>
    <automated>ls tests/atlas.spec.js</automated>
  </verify>
  <done>Test scaffold exists and is ready to verify Task 1 and Task 2.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Shelf-Packing and Atlas Management</name>
  <files>ui/webgl/atlas.js</files>
  <action>
    Create a `TextureAtlas` class that manages a 2048x2048 WebGL texture.
    - Implement a simple "Shelf Packing" algorithm to manage 2D space within the atlas.
    - Maintain a `Map` of `char+font` to UV coordinates `{u, v, width, height}`.
    - Handle atlas-full conditions by clearing and rebuilding (simple strategy).
    - Use `gl.texImage2D` for initial allocation and `gl.texSubImage2D` for dynamic updates.
  </action>
  <verify>
    <automated>npx playwright test tests/atlas.spec.js</automated>
  </verify>
  <done>TextureAtlas class can pack multiple items and return UVs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SDF Generation Logic</name>
  <files>ui/webgl/atlas.js</files>
  <action>
    Implement an `SDFGenerator` utility within `atlas.js` (or as a separate helper).
    - Use `OffscreenCanvas` (or standard Canvas) to draw a glyph at a base resolution (e.g., 64px).
    - Implement a 2D Euclidean Distance Transform (EDT) or a simplified distance transform to convert the alpha channel to an SDF.
    - Ensure the generator accounts for "padding" around the glyph to prevent clipping of the SDF effect.
    - Reference "TinySDF" logic as suggested in research.
  </action>
  <verify>
    <automated>npx playwright test tests/atlas.spec.js</automated>
  </verify>
  <done>SDF data is correctly generated for a given character and font.</done>
</task>

</tasks>

<verification>
Manual verification: Inspect the generated atlas texture using a WebGL debugger (like Spector.js) to confirm glyphs are packed and rendered as SDFs.
</verification>

<success_criteria>
1. A single 2048x2048 texture contains multiple math glyphs.
2. `getGlyph(char, font)` returns valid UV coordinates and triggers generation if missing.
3. Memory usage for the atlas is constant after initial allocation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-webgl-typography-texturing/08-01-SUMMARY.md`
</output>
