---
phase: 08-advanced-webgl-typography-texturing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [ui/webgl/typography.js, tests/typography.spec.js]
autonomous: true
requirements: [REQ-WGL-04]
must_haves:
  truths:
    - "Every glyph rendered in MathLive can be located in the DOM"
    - "Bounding boxes for glyphs are correctly extracted in CSS pixels"
    - "Changes in the MathLive editor trigger a re-sync of WebGL layout data"
  artifacts:
    - path: "ui/webgl/typography.js"
      provides: "Layout extraction from MathLive Shadow DOM"
    - path: "tests/typography.spec.js"
      provides: "Typography extraction tests"
  key_links:
    - from: "ui/webgl/typography.js"
      to: "math-field"
      via: "Shadow DOM traversal"
---

<objective>
Implement a layout extraction system that bridge the gap between MathLive's complex DOM-based mathematical layout and the WebGL rendering layer. This will provide the precise positioning data needed for the Batch Renderer.

Purpose: Sync MathLive layout with WebGL.
Output: `ui/webgl/typography.js` utility and `tests/typography.spec.js` unit test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-advanced-webgl-typography-texturing/08-RESEARCH.md
@ui/uimanager.js
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Typography Unit Test Scaffold</name>
  <files>tests/typography.spec.js</files>
  <action>
    Create a new Playwright test file `tests/typography.spec.js`.
    - Set up a test page with a MathLive `math-field`.
    - Implement a basic verification for `TypographyManager` API.
    - Add tests for coordinate extraction accuracy compared to DOM positions.
  </action>
  <verify>
    <automated>ls tests/typography.spec.js</automated>
  </verify>
  <done>Test scaffold exists and is ready to verify Task 1 and Task 2.</done>
</task>

<task type="auto">
  <name>Task 1: Implement MathLive Shadow DOM Traversal</name>
  <files>ui/webgl/typography.js</files>
  <action>
    Implement a `TypographyManager` class that can traverse the Shadow DOM of a `math-field`.
    - Use `document.createTreeWalker` to find text nodes within the shadow root.
    - For each text node, identify its character content and its parent's CSS `font` style.
    - Use `getBoundingClientRect()` on the parent element to get the precise layout box.
    - Account for the scroll position of the scientific rows container.
  </action>
  <verify>
    <automated>npx playwright test tests/typography.spec.js</automated>
  </verify>
  <done>List of glyph metadata (char, font, x, y, width, height) can be extracted from any scientific row.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Synchronization Bridge</name>
  <files>ui/webgl/typography.js</files>
  <action>
    Implement a synchronization mechanism that connects `math-field` events to the layout extractor.
    - Listen for `input` and `selection-change` events on all scientific math-fields.
    - Batch layout updates to occur once per frame (e.g., via `requestAnimationFrame`) to avoid redundant calculations.
    - Provide a method to retrieve all glyphs currently visible in the scientific row viewport.
  </action>
  <verify>
    <automated>npx playwright test tests/typography.spec.js</automated>
  </verify>
  <done>WebGL renderer is notified whenever MathLive content or layout changes.</done>
</task>

</tasks>

<verification>
Manual verification: Use `console.table(extractGlyphs(mf))` in the browser console to confirm correct bounding boxes are being extracted.
</verification>

<success_criteria>
1. Glyph coordinates are accurate to 0.5px.
2. Layout updates are fast (under 2ms per row).
3. The system correctly identifies KaTeX font families.
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-webgl-typography-texturing/08-02-SUMMARY.md`
</output>
