---
phase: 10-full-migration-final-verification
plan: 02
type: execute
wave: 2
depends_on: [10-full-migration-final-verification-01]
files_modified: [tests/parity.spec.js, ui/webgl/context.js, tests/mobile.spec.js]
autonomous: true
requirements: [REQ-VER-02, REQ-VER-03]

must_haves:
  truths:
    - "Visual parity tests show 0% diff between WebGL and Legacy DOM in Split-View"
    - "Touch interactions correctly trigger calculator actions via Ghost DOM"
    - "WebGL context loss/restoration works without visual corruption"
  artifacts:
    - path: "tests/parity.spec.js"
      provides: "Automated parity testing suite"
    - path: "tests/mobile.spec.js"
      provides: "Mobile touch verification"
  key_links:
    - from: "tests/parity.spec.js"
      to: "ui/uimanager.js"
      via: "toggleParityMode to trigger comparison"
---

<objective>
Execute objective parity verification between WebGL and Legacy DOM using the split-view toggle, and ensure cross-platform/mobile input stability.

Purpose: Validate that the WebGL rendering is 1:1 with the original DOM application (REQ-VER-02, REQ-VER-03).
Output: Verified parity across all calculator modes and mobile input resilience.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-full-migration-final-verification/10-RESEARCH.md
@ui/uimanager.js
@ui/webgl/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visual Parity Audit (REQ-VER-02)</name>
  <files>tests/parity.spec.js</files>
  <action>
    - Create `tests/parity.spec.js` using Playwright.
    - Implement tests that cycle through `toggleParityMode()` and capture snapshots of WebGL and Legacy DOM modes.
    - Use `pixelmatch` or Playwright `toHaveScreenshot()` to verify that the WebGL overlay aligns perfectly with the legacy DOM structure.
    - Test across all modes: Standard, Scientific, and Cards.
  </action>
  <verify>
    <automated>npx playwright test tests/parity.spec.js</automated>
    - Ensure all tests pass with zero diff.
  </verify>
  <done>Automated visual parity is verified across all app modes.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile Touch & Context Loss Resilience (REQ-VER-03)</name>
  <files>ui/webgl/context.js, tests/mobile.spec.js</files>
  <action>
    - Update `ui/webgl/context.js`: Cap `devicePixelRatio` at 2.0 to prevent thermal throttling (per RESEARCH.md).
    - Create `tests/mobile.spec.js` to simulate touch events (long press, multi-touch) and verify Ghost DOM hit-testing works.
    - Add a test case in `tests/mobile.spec.js` that triggers `WEBGL_lose_context` and verifies recovery via `webglcontextrestored`.
  </action>
  <verify>
    <automated>npx playwright test tests/mobile.spec.js</automated>
    - Test that touch inputs trigger correct calculator results.
    - Test that app remains interactive after context restoration.
  </verify>
  <done>Mobile input stability and WebGL context resilience are verified.</done>
</task>

</tasks>

<verification>
Automated: `npx playwright test tests/parity.spec.js tests/mobile.spec.js` passes.
</verification>

<success_criteria>
1. Visual Parity: Zero visual regression compared to the original legacy rendering.
2. Context Resilience: App survives and restores from WebGL context loss.
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-migration-final-verification/10-02-SUMMARY.md`
</output>
