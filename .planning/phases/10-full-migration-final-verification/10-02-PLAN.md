---
phase: 10-full-migration-final-verification
plan: 02
type: execute
wave: 2
depends_on: [10-full-migration-final-verification-01]
files_modified: [tests/parity.spec.js, ui/webgl/context.js, tests/mobile.spec.js]
autonomous: true
requirements: [REQ-VER-02, REQ-VER-03]

must_haves:
  truths:
    - "Automated visual parity tests pass with zero diff"
    - "Touch and gesture interactions are verified on mobile emulators"
    - "WebGL context loss is handled without app crash"
  artifacts:
    - path: "tests/parity.spec.js"
      provides: "Visual regression testing suite"
    - path: "tests/mobile.spec.js"
      provides: "Touch and gesture verification"
  key_links:
    - from: "tests/parity.spec.js"
      to: "Playwright toHaveScreenshot"
      via: "expect(page).toHaveScreenshot()"
---

<objective>
Ensure visual and interaction parity across platforms, focusing on mobile responsiveness and WebGL resilience.

Purpose: Guarantee that the 100% WebGL migration hasn't introduced regressions.
Output: Verified mobile interaction layer and an automated visual testing suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-full-migration-final-verification/10-RESEARCH.md
@ui/webgl/context.js
@ui/webgl/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated Visual Parity (Playwright)</name>
  <files>tests/parity.spec.js</files>
  <action>
    - Create `tests/parity.spec.js` using Playwright.
    - Implement tests that navigate to the app, wait for WebGL rendering to stabilize, and take screenshots.
    - Use `expect(page).toHaveScreenshot()` to establish baselines and verify parity.
    - Test across different calculator modes (Standard, Scientific, Cards) and themes.
  </action>
  <verify>
    - `npx playwright test tests/parity.spec.js` passes.
  </verify>
  <done>Automated visual regression suite is functional and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile Gestures & Context Resilience Audit</name>
  <files>ui/webgl/context.js, tests/mobile.spec.js</files>
  <action>
    - Update `ui/webgl/context.js`: Cap `devicePixelRatio` at 2.0 in the `resize()` method to prevent thermal throttling on high-DPI mobile devices (per RESEARCH.md Pitfall 2).
    - Create `tests/mobile.spec.js` to simulate touch events and verify that the Ghost DOM correctly handles multi-touch and long-press (if applicable).
    - Add a test case in `tests/mobile.spec.js` that manually triggers `WEBGL_lose_context` extension to verify that the app re-initializes correctly via `onRestored`.
  </action>
  <verify>
    - `npx playwright test tests/mobile.spec.js` passes.
    - App recovers from simulated context loss.
  </verify>
  <done>Mobile performance is optimized and WebGL resilience is verified.</done>
</task>

</tasks>

<verification>
Run all Playwright tests: `npx playwright test tests/parity.spec.js tests/mobile.spec.js`.
</verification>

<success_criteria>
1. Visual Parity: Manual audit confirms zero visual regressions across all calculator modes.
2. Input Parity: Touch and gesture interactions are fully verified on physical mobile devices.
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-migration-final-verification/10-02-SUMMARY.md`
</output>
