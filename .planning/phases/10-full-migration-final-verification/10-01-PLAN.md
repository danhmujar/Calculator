---
phase: 10-full-migration-final-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ui/uimanager.js, ui/webgl/renderer.js, ui/renderer.js, ui/styles.css]
autonomous: true
requirements: [REQ-VER-01, REQ-ARCH-01]

must_haves:
  truths:
    - "All UI components are rendered on the WebGL canvas"
    - "DOM elements are invisible (Ghost DOM) but still receive events"
    - "WebGL canvas is a top-level overlay with pointer-events: none"
    - "Legacy DOM rendering logic is removed from ui/renderer.js"
  artifacts:
    - path: "ui/webgl/renderer.js"
      provides: "Full scene WebGL rendering"
    - path: "ui/uimanager.js"
      provides: "Ghost DOM configuration and Top-Level Overlay setup"
  key_links:
    - from: "ui/uimanager.js"
      to: "ui/webgl/renderer.js"
      via: "renderer.schedule(() => webglRenderer.render())"
---

<objective>
Transition the application from an "Underlay" WebGL model to a 100% "Top-Level Overlay" WebGL rendering model, effectively removing the legacy DOM rendering layer.

Purpose: Complete the WebGL migration by making WebGL the primary rendering engine.
Output: A visually identical app running 100% on GPU, with an invisible "Ghost DOM" for accessibility and events.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-full-migration-final-verification/10-RESEARCH.md
@ui/uimanager.js
@ui/webgl/renderer.js
@ui/renderer.js
@ui/styles.css

<interfaces>
From ui/webgl/renderer.js:
```typescript
export class WebGLRenderer {
    render(): void;
    pushRect(rect: Rect, color: number[], radius: number, id: string): void;
    pushGlyph(char: string, font: string, x: number, y: number, color: number[], size: number, id: string): void;
    flush(): void;
}
```
From services/layout.js:
```typescript
export class LayoutManager {
    observe(element: HTMLElement, id: string): void;
    getRect(element: HTMLElement): Rect;
    refreshAll(): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ghost DOM & Top-Level Canvas Transition</name>
  <files>ui/uimanager.js, ui/styles.css</files>
  <action>
    - Update `ui/uimanager.js`: Change WebGL canvas placement from `.prepend()` to `.append()` or a high z-index overlay.
    - Set `this.webgl.canvas.style.pointerEvents = 'none'` to allow clicks to pass through to the DOM.
    - In `ui/styles.css`, implement a `.ghost-mode` class (or apply to `.webgl-active` which is now permanent) that sets `opacity: 0` on UI elements (buttons, inputs, display) while keeping `pointer-events: auto`.
    - Ensure the main container background is handled by WebGL or a static base layer so the app isn't just a black screen.
    - Remove `setupWebGLToggle` logic; WebGL rendering should now be the only mode.
  </action>
  <verify>
    - Inspect DOM: Elements have opacity 0 but are still clickable.
    - Canvas is on top in the element tree.
  </verify>
  <done>Ghost DOM is active and WebGL canvas is the top-level overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Full Scene WebGL Rendering & Legacy Removal</name>
  <files>ui/webgl/renderer.js, ui/renderer.js, ui/uimanager.js</files>
  <action>
    - Expand `WebGLRenderer.renderStandardMode` in `ui/webgl/renderer.js` to iterate through ALL tracked layout elements (buttons, cards, display) and render them as SDF primitives.
    - Use `this.themeColors` to match the current theme.
    - Update `ui/uimanager.js`: Remove `fitDisplayText` DOM updates; use `LayoutManager` to track the Ghost Display and let WebGL render the text via `TypographyManager`.
    - Clean up `ui/renderer.js`: Remove legacy `schedule` and `fitDisplayText` DOM logic that is no longer needed.
    - Remove any "side-by-side" or "toggle" references in `UIManager` (per REQ-VER-01 removal).
  </action>
  <verify>
    - App renders all buttons and text correctly in WebGL.
    - No DOM text or button styles are visible (except via Ghost DOM opacity 0).
    - Buttons still trigger calculations when clicked.
  </verify>
  <done>Legacy DOM rendering layer is completely swapped for 100% WebGL rendering.</done>
</task>

</tasks>

<verification>
Manual check: Use DevTools to set `opacity: 1` on the canvas and `opacity: 0.5` on the Ghost DOM to verify perfect alignment.
Automated check: `npm test tests/renderer.spec.js` passes.
</verification>

<success_criteria>
1. Clean Swap: The legacy DOM rendering layer is removed; the app runs 100% on Raw WebGL 2.0.
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-migration-final-verification/10-01-SUMMARY.md`
</output>
