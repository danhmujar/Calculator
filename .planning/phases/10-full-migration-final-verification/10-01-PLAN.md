---
phase: 10-full-migration-final-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ui/uimanager.js, ui/webgl/renderer.js, ui/renderer.js, ui/styles.css]
autonomous: true
requirements: [REQ-VER-01]

must_haves:
  truths:
    - "WebGL-vs-Legacy DOM split-view toggle is functional"
    - "DOM elements can be toggled between visible and Ghost DOM (opacity 0)"
    - "WebGL canvas can be toggled between underlay (z-index -1) and overlay (z-index 1)"
  artifacts:
    - path: "ui/uimanager.js"
      provides: "Split-view toggle and rendering mode controller"
    - path: "ui/styles.css"
      provides: ".ghost-mode styling for legacy elements"
  key_links:
    - from: "ui/uimanager.js"
      to: "ui/webgl/renderer.js"
      via: "toggle functionality (per REQ-VER-01)"
---

<objective>
Implement the side-by-side/split-view toggle for WebGL vs Legacy DOM to support parity testing, and transition the WebGL canvas to a configurable overlay model.

Purpose: Provide the necessary tools for objective visual parity verification (REQ-VER-01).
Output: A functional split-view toggle and "Ghost DOM" capability without removing legacy logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-full-migration-final-verification/10-RESEARCH.md
@ui/uimanager.js
@ui/webgl/renderer.js
@ui/renderer.js
@ui/styles.css

<interfaces>
From ui/webgl/renderer.js:
```typescript
export class WebGLRenderer {
    render(): void;
    pushRect(rect: Rect, color: number[], radius: number, id: string): void;
    pushGlyph(char: string, font: string, x: number, y: number, color: number[], size: number, id: string): void;
    flush(): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parity Toggle & Overlay Transition (REQ-VER-01)</name>
  <files>ui/uimanager.js, ui/styles.css</files>
  <action>
    - Update `ui/uimanager.js`: Implement a `toggleParityMode()` method that cycles through:
      1. WebGL-Only (Overlay: z-index 1, Legacy: opacity 0)
      2. Split-View (Canvas width 50%, Legacy visible in other 50%)
      3. Legacy-Only (Canvas hidden, Legacy: opacity 1)
    - Set `this.webgl.canvas.style.pointerEvents = 'none'` when in Overlay mode to allow clicks to pass to the Ghost DOM.
    - In `ui/styles.css`, add `.ghost-mode` class: `opacity: 0; pointer-events: auto;`.
    - Implement a debug key listener (e.g., `Shift+P`) or a temporary UI button to trigger the toggle.
  </action>
  <verify>
    <automated>npm test tests/integration.spec.js</automated>
    - Manual: Press Shift+P and verify the rendering mode cycles correctly.
  </verify>
  <done>Split-view toggle is implemented and functional per REQ-VER-01.</done>
</task>

<task type="auto">
  <name>Task 2: Full Scene WebGL Coverage</name>
  <files>ui/webgl/renderer.js, ui/uimanager.js</files>
  <action>
    - Expand `WebGLRenderer.renderStandardMode` in `ui/webgl/renderer.js` to iterate through ALL tracked layout elements (buttons, cards, display) and render them.
    - Ensure `ui/uimanager.js` correctly synchronizes all visible element states to the WebGL rendering loop.
    - Do NOT remove any legacy `ui/renderer.js` code yet; ensure legacy rendering still functions when toggled to Legacy-Only mode.
  </action>
  <verify>
    <automated>npm test tests/renderer.spec.js</automated>
    - WebGL renders all UI components; alignment is verified by toggling Split-View.
  </verify>
  <done>WebGL scene covers 100% of the UI while maintaining legacy compatibility.</done>
</task>

</tasks>

<verification>
Manual check: Use DevTools to verify that the `.ghost-mode` class is applied correctly.
Automated check: `npm test` passes.
</verification>

<success_criteria>
1. Parity Toggle: A split-view or overlay toggle exists to compare WebGL and DOM rendering side-by-side.
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-migration-final-verification/10-01-SUMMARY.md`
</output>
