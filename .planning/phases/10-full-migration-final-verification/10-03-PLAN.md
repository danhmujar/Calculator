---
phase: 10-full-migration-final-verification
plan: 03
type: execute
wave: 3
depends_on: [10-full-migration-final-verification-02]
files_modified: [ui/renderer.js, ui/uimanager.js, services/store.js, services/calculator.js, tests/modular.spec.js, .planning/phases/10-full-migration-final-verification/VERIFICATION.md]
autonomous: true
requirements: [REQ-TEST-01, REQ-TEST-02]

must_haves:
  truths:
    - "Legacy DOM rendering logic is fully removed from codebase"
    - "Modular services (Store, Calculator, Layout) have 100% test coverage"
    - "Lighthouse scores for Performance and PWA are above 90%"
  artifacts:
    - path: "ui/renderer.js"
      provides: "Clean, non-DOM based renderer interface"
    - path: "tests/modular.spec.js"
      provides: "Comprehensive service test suite"
  key_links:
    - from: "ui/uimanager.js"
      to: "ui/webgl/renderer.js"
      via: "primary rendering path (legacy removed)"
---

<objective>
Final legacy code removal, modular service test expansion, and project sign-off.

Purpose: Complete the full migration by purging legacy logic and ensuring service reliability (REQ-TEST-01).
Output: A clean, 100% WebGL-driven codebase and a detailed final VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@ui/renderer.js
@services/store.js
@services/calculator.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legacy Code Removal & Asset Cleanup</name>
  <files>ui/renderer.js, ui/uimanager.js</files>
  <action>
    - Remove legacy `ui/renderer.js` DOM manipulation logic (e.g., `fitDisplayText`, DOM-based `render` calls).
    - Purge `toggleParityMode()` from `ui/uimanager.js` and fix the app to permanently use "Ghost DOM" (opacity 0) + "WebGL Overlay" (z-index 1).
    - Remove unused CSS styles associated with legacy rendering from `ui/styles.css`.
  </action>
  <verify>
    <automated>npm run build</automated>
    - Verify app still renders correctly in WebGL mode after cleanup.
  </verify>
  <done>Legacy rendering logic is removed from the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Modular Service Test Suite (REQ-TEST-01)</name>
  <files>services/store.js, services/calculator.js, tests/modular.spec.js</files>
  <action>
    - Create `tests/modular.spec.js` to comprehensively test the core services (Store, Calculator, Event Bus) in isolation.
    - Focus on edge cases in the scientific calculator logic and state transitions in the store.
    - Ensure all public methods in modular services are exercised by tests.
  </action>
  <verify>
    <automated>npm test tests/modular.spec.js</automated>
    - Ensure all service tests pass.
  </verify>
  <done>Core modular services have a comprehensive, automated test suite.</done>
</task>

<task type="auto">
  <name>Task 3: PWA & Performance Final Audit (REQ-TEST-02)</name>
  <files>.planning/phases/10-full-migration-final-verification/VERIFICATION.md</files>
  <action>
    - Run Lighthouse audit for Performance, Accessibility, and PWA.
    - Perform a 100+ scientific row stress test to verify sustained 60FPS.
    - Finalize `VERIFICATION.md` documenting performance metrics and requirement fulfillment.
  </action>
  <verify>
    <automated>npx lighthouse http://localhost:5173 --only-categories=performance,pwa,accessibility --view</automated>
    - Verify scores are >= 90.
  </verify>
  <done>Final project state is audited and verified against requirements.</done>
</task>

</tasks>

<verification>
Automated: All tests pass and Lighthouse scores meet targets.
</verification>

<success_criteria>
1. Legacy Cleanup: No legacy DOM rendering logic remains.
2. Service Testing: Comprehensive modular test suite exists (REQ-TEST-01).
3. Final Sign-off: Final VERIFICATION.md is completed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-migration-final-verification/10-03-SUMMARY.md`
</output>
