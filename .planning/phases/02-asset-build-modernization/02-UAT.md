# Phase 02 UAT: Asset & Build Modernization

- **Phase Goal:** Reduce file sizes and automate the build/PWA pipeline.
- **Date:** 2026-03-30
- **Status:** PASS

| ID | Scenario | Steps | Expected Result | Actual Result | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| UAT-06 | SVG Sprite Loading | 1. Open App 2. Verify all UI icons (settings, history, scientific toggle) are visible | Icons are loaded from `/assets/sprites.svg` and rendered correctly. | All icons rendered correctly. Path fixed from `./public/assets/` to `/assets/` per Vite recommendations. | PASS |
| UAT-07 | PWA Manifest & Icons | 1. Check browser dev tools (Application tab) | Manifest is detected and all icons are correctly referenced. | Manifest and all icons correctly detected in DevTools. | PASS |
| UAT-08 | Service Worker Registration | 1. Open App 2. Check dev tools (Service Workers) | `sw.js` is registered successfully from the build artifacts. | `dist/sw.js` and `dist/workbox-*.js` generated by PWA plugin. | PASS |
| UAT-09 | Production Asset Hashing | 1. Run `npm run build` 2. Inspect `dist/assets` | All JS and CSS files have content hashes (e.g., `index-ABC123.js`). | Verified hashes on `index-*.js`, `index-*.css`, and `sprites-*.svg`. | PASS |
| UAT-10 | Dependency Migration Logic | 1. Perform calculation 2. Toggle Scientific mode | `mathjs` and `mathlive` function correctly as bundled npm packages. | Calculations and MathLive rendering verified. | PASS |

## UAT Summary
All Phase 02 UAT scenarios passed. The asset externalization, PWA modernization, and dependency overhaul have been successfully validated through both automated checks and manual verification.
