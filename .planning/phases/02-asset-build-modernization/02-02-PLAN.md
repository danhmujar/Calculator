---
phase: 02-asset-build-modernization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [package.json, index.html, services/app.js, ui/styles.css]
autonomous: true
requirements: [P2-T3]

must_haves:
  truths:
    - "No external CDN scripts for mathjs or mathlive remain in index.html"
    - "Scientific mode calculations function correctly using local npm packages"
    - "MathLive math-fields are correctly rendered and interactive"
  artifacts:
    - path: "package.json"
      provides: "npm dependencies for math engine"
      contains: "\"mathjs\": \"^14.x\""
  key_links:
    - from: "services/app.js"
      to: "mathjs"
      via: "import { create, addDependencies, evaluateDependencies, numberDependencies } from 'mathjs'"
---

<objective>
Migrate mathjs and mathlive from CDN scripts to npm-managed dependencies with a focus on tree-shaking and performance.

Purpose: Improve reliability, enable tree-shaking for smaller bundles, and support fully offline development.
Output: Updated package.json and refactored application logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asset-build-modernization/02-CONTEXT.md
@.planning/phases/02-asset-build-modernization/02-RESEARCH.md
@package.json
@index.html
@services/app.js
@ui/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and clean index.html</name>
  <files>package.json, index.html</files>
  <action>
    - Install mathjs and mathlive: `npm install mathjs mathlive`.
    - Set `"type": "module"` in package.json to reflect the project's use of ES modules.
    - Remove CDN-related &lt;script&gt;, &lt;link rel="preconnect"&gt;, and SRI metadata from index.html.
    - Remove any lazy-loading pre-fetch logic from index.html or services/app.js if no longer needed.
    - Per D-06.
  </action>
  <verify>
    <automated>grep -E "mathjs|mathlive" package.json</automated>
  </verify>
  <done>Dependencies installed and CDN references removed.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor services/app.js and ui/styles.css for npm packages</name>
  <files>services/app.js, ui/styles.css</files>
  <action>
    - Import mathjs and mathlive directly in services/app.js.
    - CRITICAL: Implement tree-shaking for mathjs using the pattern from RESEARCH.md: `import { create, addDependencies, evaluateDependencies, numberDependencies } from 'mathjs'`. Do NOT use `import { create, all }` as it violates Decision D-07 and the User's choice.
    - Remove the lazy-loading logic (`ensureSciLibs`, `loadScript`, `initIdleBackgroundFetch`) and replace it with direct, synchronous usage (since Vite will handle the bundling).
    - Ensure MathfieldElement is correctly initialized/registered if required by the npm version of mathlive.
    - CRITICAL: Add the following CSS rule to ui/styles.css to fix the MathLive FOUC: `math-field:not(:defined) { visibility: hidden; }`.
    - Per D-07.
  </action>
  <verify>
    <automated>grep -E "import.*from 'mathjs'" services/app.js</automated>
  </verify>
  <done>Application logic uses npm-managed libraries with tree-shaking; FOUC fix applied.</done>
</task>

</tasks>

<verification>
Ensure scientific mode can still evaluate expressions (e.g., '2 + 2' should result in 4) and that math-fields are correctly styled and responsive. Verify no FOUC for math-fields.
</verification>

<success_criteria>
- CDN dependencies are completely removed.
- Bundle size remains optimized through tree-shaking.
- Application functionality remains intact.
- Math-field FOUC is eliminated.
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-build-modernization/02-02-SUMMARY.md`
</output>
