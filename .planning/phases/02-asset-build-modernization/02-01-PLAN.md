---
phase: 02-asset-build-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/assets/sprites.svg, index.html, services/app.js]
autonomous: true
requirements: [P2-T1]

must_haves:
  truths:
    - "index.html has no inline SVGs (except minimal wrapper or special cases)"
    - "Icons render correctly using <use> reference"
    - "CSS variable theming still applies to icons"
  artifacts:
    - path: "public/assets/sprites.svg"
      provides: "Consolidated SVG sprite sheet"
      contains: "<symbol id=\"calculator-svg\""
  key_links:
    - from: "index.html"
      to: "public/assets/sprites.svg"
      via: "<use xlink:href=\"/Calculator/assets/sprites.svg#id\">"
---

<objective>
Extract ~24 inline SVGs from index.html and dynamic generator functions in services/app.js into a consolidated SVG sprite sheet.

Purpose: Reduce HTML payload size, improve caching of static assets, and maintain a cleaner codebase.
Output: public/assets/sprites.svg and updated source files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asset-build-modernization/02-CONTEXT.md
@.planning/phases/02-asset-build-modernization/02-RESEARCH.md
@index.html
@services/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SVGs to public/assets/sprites.svg</name>
  <files>public/assets/sprites.svg</files>
  <action>
    - Create public/assets/sprites.svg with a root &lt;svg xmlns="http://www.w3.org/2000/svg"&gt;&lt;defs&gt;...&lt;/defs&gt;&lt;/svg&gt; structure.
    - Extract all &lt;svg&gt; tags from index.html and convert them to &lt;symbol&gt; tags inside the &lt;defs&gt; block.
    - Give each symbol a unique ID (e.g., id="icon-calc", id="icon-palette", id="icon-sun", etc.).
    - Remove width/height attributes from &lt;symbol&gt; and ensure viewBox is preserved.
    - Also extract SVGs generated in services/app.js (e.g., in createCopySvg, deleteRow logic, and createMathActions) and add them to the sprite sheet.
    - Per D-01 and D-02.
  </action>
  <verify>
    <automated>grep "&lt;symbol id=" public/assets/sprites.svg | wc -l</automated>
  </verify>
  <done>Sprite sheet contains all ~24 icons as symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html and services/app.js to use sprite sheet</name>
  <files>index.html, services/app.js</files>
  <action>
    - Replace inline &lt;svg&gt; tags in index.html with:
      &lt;svg class="icon-classname" aria-hidden="true"&gt;&lt;use xlink:href="/Calculator/assets/sprites.svg#id"&gt;&lt;/use&gt;&lt;/svg&gt;
    - Ensure classes like "calculator-svg", "palette-btn-inner", etc., are preserved on the outer &lt;svg&gt; tag.
    - Update services/app.js functions to use the &lt;use&gt; pattern instead of generating SVG elements programmatically.
    - CRITICAL: Replace the programmatic delete button SVG creation in createRow (lines 178-186) and createMathActions (lines 1450-1460) with the &lt;use&gt; pattern for consistency.
    - Ensure CSS variable theming (e.g., fill: var(--primary-blue)) still works by checking that paths in the sprite sheet use fill="currentColor" or omit fill where CSS should override.
  </action>
  <verify>
    <automated>grep -c "&lt;use xlink:href=" index.html</automated>
  </verify>
  <done>HTML and JS reference the sprite sheet correctly.</done>
</task>

</tasks>

<verification>
Check browser to ensure all icons (header, keypad, cards, sidebar, about modal) are visible and correctly colored in both light and dark themes.
</verification>

<success_criteria>
- index.html file size reduced.
- public/assets/sprites.svg exists and contains all required symbols.
- No regression in icon rendering or theming.
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-build-modernization/02-01-SUMMARY.md`
</output>
