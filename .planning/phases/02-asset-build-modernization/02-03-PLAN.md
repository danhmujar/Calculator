---
phase: 02-asset-build-modernization
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified: [vite.config.js, package.json, services/app.js, public/sw.js, scripts/postbuild.js]
autonomous: true
requirements: [P2-T2]

must_haves:
  truths:
    - "Service worker is generated by vite-plugin-pwa"
    - "App shows 'New version available' toast when update is ready"
    - "Production build artifacts have hashes in filenames"
  artifacts:
    - path: "vite.config.js"
      provides: "Automated PWA and asset pipeline"
      contains: "VitePWA"
  key_links:
    - from: "services/app.js"
      to: "virtual:pwa-register"
      via: "import { registerSW } from 'virtual:pwa-register'"
---

<objective>
Modernize the PWA implementation using vite-plugin-pwa and optimize the Vite build pipeline by re-enabling asset hashing and removing manual scripts.

Purpose: Automate the service worker lifecycle, ensure reliable cache-busting, and follow modern build standards.
Output: Integrated vite-plugin-pwa and cleaned-up build scripts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asset-build-modernization/02-CONTEXT.md
@.planning/phases/02-asset-build-modernization/02-RESEARCH.md
@.planning/phases/02-asset-build-modernization/02-02-SUMMARY.md
@vite.config.js
@package.json
@services/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate vite-plugin-pwa and re-enable hashing</name>
  <files>vite.config.js, package.json</files>
  <action>
    - Install `vite-plugin-pwa` as a dev dependency.
    - Update vite.config.js to include `VitePWA` plugin with "Prompt for Update" strategy (registerType: 'prompt').
    - Configure includeAssets (icons, etc.) and workbox globPatterns for offline support.
    - Re-enable Vite's default filename hashing in vite.config.js by removing the custom rollupOptions that disabled it.
    - Per D-03, D-04, and D-05.
  </action>
  <verify>
    <automated>grep "VitePWA" vite.config.js</automated>
  </verify>
  <done>vite-plugin-pwa integrated and asset hashing re-enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Replace manual PWA logic and clean up</name>
  <files>services/app.js, public/sw.js, scripts/postbuild.js, package.json</files>
  <action>
    - Refactor services/app.js: Replace the current manual service worker registration logic with `import { registerSW } from 'virtual:pwa-register'`.
    - Map the `onNeedRefresh` and `onOfflineReady` callbacks to the existing update-toast UI components in the HTML (or new ones if necessary).
    - Remove the manual `public/sw.js` and `scripts/postbuild.js` files.
    - Remove the `postbuild` script from package.json as it is no longer required.
  </action>
  <verify>
    <automated>grep "virtual:pwa-register" services/app.js</automated>
  </verify>
  <done>Manual PWA code removed and replaced with modern plugin-driven registration.</done>
</task>

</tasks>

<verification>
Run `npm run build` and `npm run preview`. Open the app in a browser, ensure the service worker registers, then modify a file and re-build to verify the 'New version available' toast appears upon refresh.
</verification>

<success_criteria>
- Service worker generated by Vite.
- Asset filenames contain hashes.
- PWA update strategy is correctly implemented as 'prompt'.
- scripts/postbuild.js is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-build-modernization/02-03-SUMMARY.md`
</output>
