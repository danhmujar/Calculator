---
phase: 06-architectural-hardening
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified: [ui/ui.js, ui/renderer.js, tests/performance.spec.js]
autonomous: true
requirements: [REQ-SEC-01, REQ-TEST-01]
must_haves:
  truths:
    - "innerHTML is not used for dynamic content in ui/ui.js or ui/renderer.js."
    - "Application passes all automated verification tests (unit, E2E, security)."
    - "Memory footprint and state read performance meet targets."
  artifacts:
    - path: "ui/ui.js"
      provides: "Secure DOM manipulation"
  key_links:
    - from: "ui/ui.js"
      to: "textContent"
      via: "replacement for innerHTML"
---

<objective>
Finalize architectural hardening by eliminating remaining `innerHTML` usages and performing a comprehensive validation of reliability and performance across the entire application.

Purpose: Achieve final security hardening and verify phase success criteria.
Output: Hardened application ready for WebGL migration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-architectural-hardening/06-RESEARCH.md
@ui/ui.js
@ui/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate remaining innerHTML usages</name>
  <files>ui/ui.js, ui/renderer.js</files>
  <action>
    - Audit all occurrences of `innerHTML` in `ui/ui.js` and `ui/renderer.js`.
    - Replace them with safe DOM alternatives: `textContent`, `createElement`, `appendChild`, or template literals within a safe sanitizer if truly necessary.
    - Ensure all dynamic content (especially math results) is inserted safely.
  </action>
  <verify>
    <automated>! grep -r "innerHTML =" ui/</automated>
  </verify>
  <done>innerHTML eliminated from UI layer.</done>
</task>

<task type="auto">
  <name>Task 2: Perform comprehensive validation of success criteria</name>
  <files>tests/state.spec.js, tests/scientific.spec.js, tests/security.spec.js, tests/performance.spec.js</files>
  <action>
    - Run the complete test suite.
    - Verify all requirements: Modular services (REQ-ARCH-01), Secure MathJS (REQ-SEC-01), O(1) state reads (REQ-STORE-01), No race conditions (REQ-BUG-01), LRU cache (REQ-ARCH-02).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All success criteria verified via tests.</done>
</task>

</tasks>

<success_criteria>
- `innerHTML` is no longer used for dynamic DOM updates.
- All Phase 06 success criteria are met.
- The application is stable and performant.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architectural-hardening/06-05-SUMMARY.md`
</output>
