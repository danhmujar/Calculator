---
phase: 06-architectural-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/store.js]
autonomous: true
requirements: [REQ-STORE-01, REQ-STORE-02]
must_haves:
  truths:
    - "State reads perform in O(1) time without recursive cloning."
    - "Transient state (cursor position) is separated from persistent state (history)."
    - "Subscribers are notified only when their specific state segment changes."
  artifacts:
    - path: "services/store.js"
      provides: "Proxy-based state management"
  key_links:
    - from: "services/store.js"
      to: "Proxy API"
      via: "Lazy Shallow Cloning"
---

<objective>
Optimize state management in `services/store.js` using Proxy-based Lazy Shallow Cloning (Copy-on-Write) to achieve O(1) reads and stable references. Segment state into transient and persistent parts to reduce redundant updates.

Purpose: Provide a high-performance foundation for the upcoming WebGL rendering layer.
Output: Optimized `services/store.js` with structural sharing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-architectural-hardening/06-RESEARCH.md
@services/store.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Proxy-based Lazy Shallow Cloning</name>
  <files>services/store.js, tests/state.spec.js</files>
  <behavior>
    - Reading state should return a Proxy that traps property access.
    - Setting a property should only clone the path to that property (structural sharing).
    - Unchanged objects should maintain reference equality (O(1) reads).
    - Modification should trigger subscribers.
  </behavior>
  <action>
    - Refactor `Store` class in `services/store.js`.
    - Replace deep-cloning with a Proxy handler that implements Lazy Shallow Cloning per 06-RESEARCH.md.
    - Ensure `getState()` returns a reactive snapshot or the Proxy itself if efficient.
    - Avoid `JSON.parse(JSON.stringify(this.state))`.
  </action>
  <verify>
    <automated>npm test tests/state.spec.js</automated>
  </verify>
  <done>Store uses structural sharing; performance tests show O(1) read overhead.</done>
</task>

<task type="auto">
  <name>Task 2: Segment state into transient and persistent</name>
  <files>services/store.js</files>
  <action>
    - Update `initialState` to clearly separate `persistent` (rows, settings, theme) from `transient` (activeRowIndex, selection, focusState).
    - Ensure `saveToLocalStorage` only serializes the `persistent` segment.
    - Implement a `batch` update mechanism to allow multiple changes before notifying subscribers.
  </action>
  <verify>
    <automated>grep "persistent" services/store.js && grep "transient" services/store.js</automated>
  </verify>
  <done>State is segmented; LocalStorage only contains persistent data.</done>
</task>

</tasks>

<success_criteria>
- `services/store.js` uses Lazy Shallow Cloning.
- Performance tests confirm O(1) state reads.
- Transient state does not persist across refreshes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architectural-hardening/06-01-SUMMARY.md`
</output>
