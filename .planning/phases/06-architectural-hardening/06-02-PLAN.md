---
phase: 06-architectural-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [services/calculator.js, tests/security.spec.js, services/calculator.spec.js]
autonomous: true
requirements: [REQ-ARCH-01, REQ-SEC-01, REQ-SEC-02, REQ-TEST-01]
must_haves:
  truths:
    - "Calculations are performed by a dedicated service (services/calculator.js)."
    - "Math expressions cannot access global JS objects (e.g., console, window)."
    - "Access to internal mathjs properties (e.g., .constructor, .prototype) is blocked via AST traversal."
  artifacts:
    - path: "services/calculator.js"
      provides: "Secure math evaluation"
    - path: "tests/security.spec.js"
      provides: "Security verification"
  key_links:
    - from: "services/calculator.js"
      to: "mathjs"
      via: "restricted evaluate"
---

<objective>
Harden calculator security by extracting calculation logic into a dedicated service and configuring `mathjs` with restrictive security settings. Implement AST-based blocking to prevent prototype pollution and other common mathjs exploits.

Purpose: Eliminate security risks associated with user-defined mathematical expressions.
Output: Decoupled and secure `services/calculator.js`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-architectural-hardening/06-RESEARCH.md
@services/app.js
</context>

<tasks>

<task type="auto" tdd="true" status="complete">
  <name>Task 1: Extract calculation logic to services/calculator.js</name>
  <files>services/calculator.js, services/calculator.spec.js</files>
  <behavior>
    - `CalculatorService.evaluate()` returns the result of a math expression.
    - Handles basic scientific operations (sqrt, sin, cos, etc.).
    - Supports row-based evaluation with scope/variables.
    - Gracefully handles errors (division by zero, invalid expressions).
  </behavior>
  <action>
    - Create `services/calculator.js`.
    - Move all `mathjs`-related evaluation logic from `services/app.js` into this new service.
    - Define a clean interface for evaluating expressions with optional scope.
    - Initialize `mathjs` within the service with restricted settings (e.g., `predictable: true`).
  </action>
  <verify>
    <automated>npm test services/calculator.spec.js</automated>
  </verify>
  <done>Calculation logic decoupled; unit tests pass.</done>
</task>

<task type="auto" tdd="true" status="complete">
  <name>Task 2: Implement Secure Math Evaluation with AST Traversal</name>
  <files>services/calculator.js, tests/security.spec.js</files>
  <behavior>
    - Blocking `AccessorNode` in the AST prevent prototype pollution (e.g., [].constructor).
    - Disabling high-risk functions like `import`, `createUnit`, and `evaluate` within user expressions.
    - Whitelisting safe function nodes.
  </behavior>
  <action>
    - Use `math.parse` to generate an AST for every expression.
    - Implement `node.traverse` to inspect every node before evaluation.
    - Throw an error if an `AccessorNode` is found (as specified in 06-RESEARCH.md).
    - Explicitly disable unsafe `mathjs` functions in the `math` instance used by the service.
  </action>
  <verify>
    <automated>npm test tests/security.spec.js</automated>
  </verify>
  <done>Security audit tests pass; AST-based protection active.</done>
</task>

</tasks>

<success_criteria>
- Calculation logic removed from `app.js`.
- Security tests confirm that prototype pollution is blocked.
- High-risk `mathjs` functions are disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architectural-hardening/06-02-SUMMARY.md`
</output>
