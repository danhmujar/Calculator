---
phase: 06-architectural-hardening
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: [ui/uimanager.js, services/pwa.js, ui/renderer.js]
autonomous: true
requirements: [REQ-ARCH-01, REQ-ARCH-02, REQ-ARCH-03]
must_haves:
  truths:
    - "DOM orchestration logic (layout, focus, theme) is moved to ui/uimanager.js."
    - "Service Worker and PWA-specific logic is moved to services/pwa.js."
    - "Renderer text-width measurements are cached in a fixed-capacity LRU."
  artifacts:
    - path: "ui/uimanager.js"
      provides: "UI Service"
    - path: "services/pwa.js"
      provides: "PWA Service"
  key_links:
    - from: "ui/renderer.js"
      to: "LRUCache"
      via: "text width caching"
    - from: "ui/uimanager.js"
      to: "services/app.js"
      via: "orchestration"
    - from: "services/pwa.js"
      to: "services/app.js"
      via: "orchestration"
---

<objective>
Decompose remaining UI and PWA logic from the monolithic `app.js` into modular services. Implement a high-performance LRU cache for text measurements in the renderer to prevent memory leaks and improve performance.

Purpose: Modularize the application and optimize UI rendering.
Output: Decoupled UI and PWA services, and optimized text measurement cache.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-architectural-hardening/06-RESEARCH.md
@services/app.js
@ui/renderer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose UI orchestration logic to ui/uimanager.js</name>
  <files>ui/uimanager.js, services/app.js</files>
  <action>
    - Create `ui/uimanager.js`.
    - Extract UI-specific logic from `services/app.js` (e.g., layout updates, theme management, focus handling).
    - Provide a clean interface for `app.js` to coordinate UI state changes.
  </action>
  <verify>
    <automated>grep -r "UIManager" services/app.js</automated>
  </verify>
  <done>UI logic moved to UIManager; app.js logic reduced.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose PWA logic to services/pwa.js</name>
  <files>services/pwa.js, services/app.js</files>
  <action>
    - Create `services/pwa.js`.
    - Move Service Worker registration and PWA event handling (e.g., `beforeinstallprompt`, `appinstalled`) from `services/app.js`.
  </action>
  <verify>
    <automated>grep -r "PWAManager" services/app.js</automated>
  </verify>
  <done>PWA logic decoupled; app.js modularity improved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement LRUCache in ui/renderer.js</name>
  <files>ui/renderer.js, tests/performance.spec.js</files>
  <behavior>
    - `LRUCache` uses a `Map` to store text width measurements with O(1) access.
    - If the cache exceeds a fixed capacity (e.g., 1000 items), the least recently used item is evicted.
    - Ensures that `textWidthCache` does not grow indefinitely.
  </behavior>
  <action>
    - Implement a Map-based `LRUCache` class (as specified in 06-RESEARCH.md).
    - Replace the existing `Map` in `ui/renderer.js` for `textWidthCache` with the new `LRUCache` instance.
    - Set a reasonable capacity for the cache.
  </action>
  <verify>
    <automated>npm test tests/performance.spec.js</automated>
  </verify>
  <done>LRU cache active in renderer; memory footprint stabilized.</done>
</task>

</tasks>

<success_criteria>
- `UIManager` and `PWAManager` services exist and are used in `app.js`.
- Renderer uses an LRU cache for text measurements.
- App continues to work as expected after decomposition.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architectural-hardening/06-03-SUMMARY.md`
</output>
