---
phase: 06-architectural-hardening
plan: 04
type: execute
wave: 3
depends_on: [06-03]
files_modified: [services/events.js, services/app.js, ui/ui.js]
autonomous: true
requirements: [REQ-ARCH-01, REQ-BUG-01, REQ-BUG-02, REQ-BUG-03]
must_haves:
  truths:
    - "Event delegation logic is extracted to services/events.js."
    - "services/app.js acts as a lightweight service orchestrator."
    - "MathLive components initialize reliably using the 'mount' event."
  artifacts:
    - path: "services/events.js"
      provides: "Event Manager"
  key_links:
    - from: "services/app.js"
      to: "services/events.js"
      via: "orchestration"
    - from: "ui/ui.js"
      to: "mathlive"
      via: "mount listener"
    - from: "services/app.js"
      to: "ui/uimanager.js"
      via: "orchestration"
    - from: "services/app.js"
      to: "services/pwa.js"
      via: "orchestration"
---

<objective>
Complete the decomposition of the monolithic `app.js` by extracting event delegation and refactoring the entry point. Resolve restoration race conditions by implementing event-driven MathLive initialization.

Purpose: Finalize the modular architecture and improve application reliability.
Output: Refactored orchestrator and reliable event system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-architectural-hardening/06-RESEARCH.md
@services/app.js
@ui/ui.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract event delegation to services/events.js</name>
  <files>services/events.js, services/app.js</files>
  <action>
    - Create `services/events.js`.
    - Move all global event delegation and bindings from `services/app.js`.
    - Provide a clean interface for binding interactions to service actions.
  </action>
  <verify>
    <automated>grep -r "EventManager" services/app.js</automated>
  </verify>
  <done>Event delegation extracted; app.js logic simplified.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor app.js as a lightweight orchestrator</name>
  <files>services/app.js</files>
  <action>
    - Refactor `services/app.js` to be the entry point that initializes `Store`, `CalculatorService`, `UIManager`, `PWAManager`, and `EventManager`.
    - Eliminate internal logic that has been moved to services.
    - Ensure all services are properly coordinated during the application lifecycle.
  </action>
  <verify>
    <automated>node -c services/app.js</automated>
  </verify>
  <done>app.js is now a lightweight orchestrator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement event-driven MathLive initialization</name>
  <files>ui/ui.js, tests/scientific.spec.js</files>
  <behavior>
    - MathLive fields are initialized only after the `mount` event fires.
    - Use `customElements.whenDefined('math-field')` to ensure component availability.
    - Eliminate all `setTimeout` calls used for delaying component restoration.
  </behavior>
  <action>
    - Update `ui/ui.js` (specifically where scientific rows are restored) to use the event-driven pattern from 06-RESEARCH.md.
    - Remove `SCI_RESTORE_DELAY_BASE_MS` usage.
  </action>
  <verify>
    <automated>npm test tests/scientific.spec.js && npx playwright test tests/scientific.spec.js</automated>
  </verify>
  <done>Race conditions resolved in row restoration; scientific mode stable.</done>
</task>

</tasks>

<success_criteria>
- `services/app.js` logic is purely orchestrational.
- Event delegation is centralized.
- Scientific row restoration is reliable on all devices.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architectural-hardening/06-04-SUMMARY.md`
</output>
