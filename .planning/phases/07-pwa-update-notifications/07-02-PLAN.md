---
phase: 07-pwa-update-notifications
plan: 02
type: execute
wave: 2
depends_on: [07-01-PLAN.md]
files_modified: [services/pwa.js]
autonomous: true
requirements: [PWA-02]
must_haves:
  truths:
    - 'Application periodically polls version.json'
    - 'Service Worker detects mismatch between local and server versions'
  artifacts:
    - path: 'services/pwa.js'
      provides: 'Version checking and update logic'
  key_links:
    - from: 'services/pwa.js'
      to: '/version.json'
      via: 'fetch() request'
---

<objective>
Implement logic in the existing PWA service to poll the server for the latest version and alert the app when a new version is detected.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add version checking logic</name>
  <files>services/pwa.js</files>
  <action>
    - Add a function to `services/pwa.js` that fetches `/version.json` and compares the version against a stored local version.
    - Set up an interval or a check on app visibility change to poll for updates.
  </action>
  <verify>
    <automated>node -e "require('./services/pwa.js')"</automated>
  </verify>
  <done>
    Service correctly identifies version change when `/version.json` is modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Service Worker update trigger</name>
  <files>services/pwa.js</files>
  <action>
    - Ensure the existing Vite PWA `registerSW` hook is triggered correctly when an update is detected, or implement a custom logic using `navigator.serviceWorker.register`.
  </action>
  <verify>
    <automated># Manual verification via logs in browser console</automated>
  </verify>
  <done>
    Service Worker signals that a new version is available.
  </done>
</task>

</tasks>

<verification>
Simulate a version change in `public/version.json` and ensure the application logs a notification.
</verification>

<success_criteria>
Client-side code detects when a new version is deployed.
</success_criteria>
