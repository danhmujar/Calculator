---
phase: 07-pwa-update-notifications
plan: 03
type: execute
wave: 3
depends_on: [07-02-PLAN.md]
files_modified: [ui/ui.js, services/app.js]
autonomous: false
requirements: [PWA-03]
must_haves:
  truths:
    - 'User is notified via a UI toast when an update is available'
    - 'User can reload the application to apply the update'
  artifacts:
    - path: 'ui/ui.js'
      provides: 'Toast component rendering'
    - path: 'services/app.js'
      provides: 'Application state management for update notifications'
  key_links:
    - from: 'services/pwa.js'
      to: 'ui/ui.js'
      via: 'Event dispatch'
---

<objective>
Implement the UI layer for the PWA update notification and ensure the cache is busted on user reload.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add update notification toast</name>
  <files>ui/ui.js, services/app.js</files>
  <action>
    - Update `ui/ui.js` to handle a "new update available" event and display a toast notification.
    - Add a "Reload to Update" button in the toast.
  </action>
  <verify>
    <automated># Manual verification of UI components</automated>
  </verify>
  <done>
    Update notification appears in the UI when the update signal is triggered.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PWA update notification system</what-built>
  <how-to-verify>
    1. Deploy a change.
    2. Change version in `version.json`.
    3. Visit the site.
    4. Confirm that the toast appears and triggers a reload.
  </how-to-verify>
  <resume-signal>Verified update workflow</resume-signal>
</task>

</tasks>

<verification>
Ensure the toast appears as expected and that clicking "Reload" clears the outdated cache.
</verification>

<success_criteria>
User is effectively prompted to update the app, ensuring they use the latest version.
</success_criteria>
