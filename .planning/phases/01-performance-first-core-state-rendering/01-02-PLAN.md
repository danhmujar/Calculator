---
phase: 01-performance-first-core-state-rendering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [ui/renderer.js, tests/display.spec.js]
autonomous: true
requirements: [REQ-P1, REQ-P3]
must_haves:
  truths:
    - "fitDisplayText calculates font size without layout thrashing"
    - "All DOM writes are batched within a single requestAnimationFrame"
  artifacts:
    - path: "ui/renderer.js"
      provides: "rAF scheduler and optimized Canvas font measurement"
    - path: "tests/display.spec.js"
      provides: "Verification for font fitting and rendering efficiency"
  key_links:
    - from: "ui/renderer.js"
      to: "CanvasRenderingContext2D"
      via: "measureText"
---

<objective>
Implement the Performance Renderer with rAF batching and optimized Canvas-based font sizing. This eliminates layout thrashing and ensures smooth 60fps display updates, fulfilling [REQ-P1] and [REQ-P3].
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-performance-first-core-state-rendering/01-CONTEXT.md
@.planning/phases/01-performance-first-core-state-rendering/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Renderer class in ui/renderer.js</name>
  <files>ui/renderer.js</files>
  <action>
    Create a `Renderer` class to handle DOM batching and font fitting:
    - `schedule(callback)`: Batch DOM updates. Call `requestAnimationFrame` only once per frame, executing all queued callbacks.
    - Implement `fitDisplayText(text, containerWidth, options)`:
      - Use an offscreen `CanvasRenderingContext2D.measureText()` to calculate text width.
      - Calculate the required font size to fit `containerWidth` by scaling a reference size (e.g., 100px).
      - Implement a `Map` cache for measured text widths to achieve O(1) performance for repeated inputs.
      - Clamp the font size between `options.minRem` and `options.maxRem`.
      - Fallback to scientific notation if text still exceeds containerWidth at `minRem`.
    - Export an instance of `Renderer`.
  </action>
  <verify>
    <automated>node -e "import { Renderer } from './ui/renderer.js'; const r = new Renderer(); console.log(typeof r.fitDisplayText);" | grep function</automated>
  </verify>
  <done>Renderer implemented with rAF scheduler and Canvas font fitting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create display fitting tests</name>
  <files>tests/display.spec.js</files>
  <behavior>
    - Test 1: fitDisplayText returns correct font sizes for various widths.
    - Test 2: Large numbers fallback to scientific notation at minimum font size.
    - Test 3: Multiple calls to fitDisplayText within one frame only result in one DOM write (via scheduler).
  </behavior>
  <action>
    Create a Playwright test file `tests/display.spec.js`:
    - Use `page.evaluate` to test `fitDisplayText` against the real DOM or its mock.
    - Measure time taken for 1000 calls to `fitDisplayText` to verify efficiency (< 1ms).
  </action>
  <verify>
    <automated>npx playwright test tests/display.spec.js</automated>
  </verify>
  <done>Automated tests confirm font fitting and batching efficiency.</done>
</task>

</tasks>

<verification>
- Run `npx playwright test tests/display.spec.js` to ensure all tests pass.
</verification>

<success_criteria>
- `ui/renderer.js` exists and exports a functional `Renderer` instance.
- `fitDisplayText` accurately calculates font sizes using Canvas measurement.
- Performance tests show font sizing logic avoids layout thrashing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-performance-first-core-state-rendering/01-02-SUMMARY.md`
</output>
