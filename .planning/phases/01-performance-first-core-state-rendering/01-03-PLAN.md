---
phase: 01-performance-first-core-state-rendering
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [services/app.js, ui/styles.css]
autonomous: true
requirements: [REQ-P3, REQ-P4, REQ-M1]
must_haves:
  truths:
    - "Application logic is decoupled from DOM state"
    - "Eye-tracking is updated via CSS variables"
  artifacts:
    - path: "services/app.js"
      provides: "Business logic layer integrated with Store and Renderer"
    - path: "ui/styles.css"
      provides: "CSS variables for hardware-accelerated pupil movement"
  key_links:
    - from: "services/app.js"
      to: "services/store.js"
      via: "import and subscribe"
    - from: "services/app.js"
      to: "ui/renderer.js"
      via: "schedule"
---

<objective>
Refactor `services/app.js` into a clean Business Logic Layer that orchestrates the `Store` and `Renderer`. This fulfills the 3-tier architecture requirement [REQ-M1] and optimizes eye-tracking performance [REQ-P4].
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-performance-first-core-state-rendering/01-CONTEXT.md
@.planning/phases/01-performance-first-core-state-rendering/01-RESEARCH.md
@.planning/phases/01-performance-first-core-state-rendering/01-01-SUMMARY.md
@.planning/phases/01-performance-first-core-state-rendering/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Eye-Tracking and Styles</name>
  <files>ui/styles.css</files>
  <action>
    Modify `ui/styles.css` to use CSS variables for eye-tracking:
    - Add `--pupil-x: 0px; --pupil-y: 0px;` to the `.pupil` class or its container.
    - Set `.pupil { transform: translate(var(--pupil-x), var(--pupil-y)); }`.
    - Ensure the transition is smooth by removing direct `left`/`top` manipulations if any.
  </action>
  <verify>
    <automated>grep -q "var(--pupil-x)" ui/styles.css</automated>
  </verify>
  <done>Styles updated for hardware-accelerated eye-tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor app.js to Business Logic Layer</name>
  <files>services/app.js</files>
  <action>
    Perform a major refactor of `services/app.js`:
    - Convert from IIFE to standard ESM imports.
    - Import `store` and `renderer`.
    - Transition all manual DOM-scraping logic (e.g., `saveState`) to purely functional state management using `store.setState`.
    - Subscribe a render function to the `store` that uses `renderer.schedule` to update the DOM.
    - Update eye-tracking event listeners to call `renderer.schedule` and update CSS variables `--pupil-x` and `--pupil-y`.
    - Refactor `fitDisplayText` usage to use the new `renderer.fitDisplayText`.
    - Ensure all user actions update the `store` instead of directly modifying the DOM.
  </action>
  <verify>
    <automated>npx playwright test tests/state.spec.js tests/display.spec.js</automated>
  </verify>
  <done>app.js refactored to use Store and Renderer, achieving clear separation of concerns.</done>
</task>

</tasks>

<verification>
- Perform manual regression testing of standard and scientific calculator modes.
- Verify `localStorage` persistence and recovery works.
- Check eye-tracking smoothness and font-fitting efficiency.
- Run all Playwright tests.
</verification>

<success_criteria>
- Eye-tracking uses CSS variables for smoother movement.
- Application logic no longer scrapes the DOM for state.
- Codebase follows the 3-tier architecture (Store, Renderer, App).
</success_criteria>

<output>
After completion, create `.planning/phases/01-performance-first-core-state-rendering/01-03-SUMMARY.md`
</output>
