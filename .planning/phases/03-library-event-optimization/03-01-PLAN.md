---
phase: 03-library-event-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/app.js]
autonomous: true
requirements: [REQ-B4, P3-T1]
must_haves:
  truths:
    - "mathjs/number is used instead of full mathjs"
    - "mathlive is lazy-loaded on-demand"
    - "A loading state/toast is shown during MathLive loading"
    - "Scientific mode functions correctly after lazy-load"
  artifacts:
    - path: "services/app.js"
      provides: "Library optimization and lazy-loading"
  key_links:
    - from: "services/app.js"
      to: "mathjs/number"
      via: "import { create, allDependencies } from 'mathjs/number'"
    - from: "services/app.js"
      to: "mathlive"
      via: "dynamic import() in activateScientificMode"
---

<objective>
Reduce the initial bundle size of the calculator by optimizing the math engine usage and lazy-loading the heavy MathLive library.

Purpose: Improve initial load time and reduce the JS execution cost on startup.
Output: Optimized services/app.js with custom mathjs build and dynamic MathLive loading.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/app.js

<interfaces>
From services/app.js:
```javascript
function activateScientificMode(sidebar, btnStd, btnSci, sciContainer) { ... }
function showToast(msg = "Copied to clipboard!") { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch to mathjs/number custom build</name>
  <files>services/app.js</files>
  <action>
    - Change `import { create, all } from 'mathjs';` to `import { create, allDependencies } from 'mathjs/number';`.
    - Change `const math = create(all);` to `const math = create(allDependencies);`.
    - This ensures only the number-based math functions are included in the bundle, excluding BigNumber, Complex, Matrix, etc.
  </action>
  <verify>
    <automated>grep "mathjs/number" services/app.js && grep "allDependencies" services/app.js</automated>
  </verify>
  <done>mathjs is initialized with allDependencies from mathjs/number.</done>
</task>

<task type="auto">
  <name>Task 2: Implement MathLive lazy-loading with Loading State</name>
  <files>services/app.js</files>
  <action>
    - Remove the top-level `import 'mathlive';`.
    - In `activateScientificMode`, before importing:
      - Show a loading toast: `showToast('Loading Scientific Engine...');`.
      - Optionally disable the button or show a spinner if applicable.
    - Perform dynamic `await import('mathlive');` only if `window.MathfieldElement` is not defined.
    - Ensure the rest of the `activateScientificMode` logic (showing the container, etc.) runs after the import completes.
  </action>
  <verify>
    <automated>! grep -E "^import 'mathlive'" services/app.js && grep "import('mathlive')" services/app.js && grep "Loading Scientific Engine" services/app.js</automated>
  </verify>
  <done>MathLive is dynamically imported with a loading toast shown only when scientific mode is activated.</done>
</task>

<task type="auto">
  <name>Task 3: Integration Verification</name>
  <files>services/app.js</files>
  <action>
    - Run the integration test suite to ensure scientific mode and math engine optimizations haven't broken the application.
    - Specifically check that the mathfield element is correctly initialized after lazy-loading.
  </action>
  <verify>
    <automated>npx playwright test tests/integration.spec.js</automated>
  </verify>
  <done>Integration tests pass, confirming lazy-loading and scientific mode functionality (per User instruction).</done>
</task>

</tasks>

<verification>
- Scientific mode functions correctly (rows can be added and calculated).
- Initial load does not include mathlive library in network trace.
- npx playwright test tests/integration.spec.js passes.
</verification>

<success_criteria>
- mathjs/number used in app.js.
- MathLive lazy-loaded via dynamic import.
- Loading state shown during MathLive load.
- No regressions in scientific calculator functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/03-library-event-optimization/03-01-SUMMARY.md`
</output>
