---
phase: 03-library-event-optimization
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [ui/eye-tracker.js, ui/styles.css, services/app.js]
autonomous: true
requirements: [REQ-P4, P3-T2, REQ-M1]
must_haves:
  truths:
    - "Eye-tracking logic is encapsulated in ui/eye-tracker.js"
    - "Pupil movement uses GPU-accelerated CSS properties"
    - "Chameleon eyes follow the cursor correctly using rAF"
    - "app.js is cleaned of legacy tracking logic"
  artifacts:
    - path: "ui/eye-tracker.js"
      provides: "Modular eye-tracking logic"
    - path: "ui/styles.css"
      provides: "Optimized pupil styles using CSS variables"
  key_links:
    - from: "services/app.js"
      to: "ui/eye-tracker.js"
      via: "import { initEyeTracking } from '../ui/eye-tracker.js'"
    - from: "ui/eye-tracker.js"
      to: "ui/renderer.js"
      via: "import { renderer } from './renderer.js'"
---

<objective>
Improve application maintainability and performance by modularizing the "Chameleon" eye-tracking feature and offloading the animation to CSS variables and GPU-accelerated transforms.

Purpose: Reduce app.js complexity and improve frame stability on mobile/low-end devices.
Output: Dedicated eye-tracker.js module and optimized CSS for pupil movements.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/app.js
@ui/renderer.js
@ui/styles.css

<interfaces>
From ui/renderer.js:
```javascript
export const renderer = new Renderer();
// renderer.schedule(callback) batches DOM/style updates via rAF
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create eye-tracker.js module</name>
  <files>ui/eye-tracker.js</files>
  <action>
    - Create `ui/eye-tracker.js`.
    - Extract the eye-tracking logic from `services/app.js`.
    - Implement `export function initEyeTracking()` which attaches the `mousemove` listener.
    - Use `renderer.schedule` from `ui/renderer.js` to batch style updates.
    - Instead of setting inline styles (top, left), use `pupil.style.setProperty('--pupil-x', ...)` and `setProperty('--pupil-y', ...)`.
    - Throttling is managed by `renderer.schedule`.
  </action>
  <verify>
    <automated>test -f ui/eye-tracker.js && grep "export function initEyeTracking" ui/eye-tracker.js && grep "setProperty('--pupil-x'" ui/eye-tracker.js</automated>
  </verify>
  <done>Eye-tracking logic is encapsulated and uses CSS variables with rAF.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Pupil CSS for GPU Acceleration</name>
  <files>ui/styles.css</files>
  <action>
    - Find `.pupil` class in `ui/styles.css`.
    - Update `transform` to use `translate3d(var(--pupil-x, 0), var(--pupil-y, 0), 0)` for GPU acceleration.
    - Add `will-change: transform;` to hint the browser about incoming updates.
    - Remove legacy `top`/`left` properties if they were used for pupil movement.
  </action>
  <verify>
    <automated>grep "translate3d" ui/styles.css && grep "will-change: transform" ui/styles.css</automated>
  </verify>
  <done>Pupil styles are optimized for GPU rendering using CSS variables.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor app.js & Performance Verification</name>
  <files>services/app.js</files>
  <action>
    - Import `initEyeTracking` from `../ui/eye-tracker.js`.
    - Call `initEyeTracking()` in the initialization phase of `app.js`.
    - Remove all existing eye-tracking logic from `app.js` (listeners, `movePupil`, constants).
    - Run the performance test suite to confirm smooth eye-tracking and GPU acceleration.
  </action>
  <verify>
    <automated>! grep "movePupil" services/app.js && npx playwright test tests/performance.spec.js</automated>
  </verify>
  <done>app.js is cleaned up and performance tests pass for eye-tracking (per User instruction).</done>
</task>

</tasks>

<verification>
- Mouse movement correctly moves pupils.
- app.js is free of eye-tracking logic.
- Performance tests pass: npx playwright test tests/performance.spec.js.
</verification>

<success_criteria>
- Eye-tracking modularized to ui/eye-tracker.js.
- Pupils use `translate3d` and `will-change`.
- npx playwright test tests/performance.spec.js passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-library-event-optimization/03-02-SUMMARY.md`
</output>
