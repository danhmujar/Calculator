---
phase: 03-library-event-optimization
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [services/app.js]
autonomous: true
requirements: [REQ-P3, REQ-M1, P3-T3]
must_haves:
  truths:
    - "Fragmented event listeners are replaced with delegated handlers"
    - "event.target.closest() is used for all delegated handlers"
    - "Interaction to Next Paint (INP) remains optimal"
    - "Zero regressions in interaction or performance"
  artifacts:
    - path: "services/app.js"
      provides: "Consolidated event handling logic"
  key_links:
    - from: "services/app.js"
      to: "#keypad"
      via: "Event delegation for calculator buttons"
    - from: "services/app.js"
      to: "#sidebar"
      via: "Event delegation for mode and control buttons"
---

<objective>
Optimize the application's responsiveness and maintainability by refactoring fragmented event listeners into regional delegation handlers. This reduces the number of active listeners and improves interaction performance.

Purpose: Improve INP (Interaction to Next Paint) and simplify event binding code.
Output: Refactored bindEvents in services/app.js using event delegation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/app.js

<interfaces>
From services/app.js:
```javascript
function addRow(btnEl, type) { ... }
function setCalcMode(mode) { ... }
function handleInput(action, value) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Keypad Button Listeners</name>
  <files>services/app.js</files>
  <action>
    - Identify individual or group listeners for calculator buttons in `#keypad`.
    - Replace them with a single delegated listener on `document.getElementById('keypad')`.
    - Use `event.target.closest('.calc-btn')` to detect button clicks.
    - Extract `data-action` and `data-value` from the button and call `handleInput(action, value)`.
  </action>
  <verify>
    <automated>grep "document.getElementById('keypad').addEventListener('click'" services/app.js && grep "closest('.calc-btn')" services/app.js</automated>
  </verify>
  <done>Keypad button listeners are consolidated into a delegated handler on #keypad.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Sidebar, Panels & Mode Listeners</name>
  <files>services/app.js</files>
  <action>
    - Identify multiple individual listeners for buttons inside `#sidebar` and `.left-panel` (history-toggle, mode-std, mode-sci, clear-tape, add-row buttons).       
    - Consolidate these into delegated listeners on `#sidebar` and `.left-panel` respectively.
    - Use `event.target.closest('[data-add-row]')` for row buttons.
    - Use `event.target.closest('[data-mode]')` or specific IDs for sidebar actions.
  </action>
  <verify>
    <automated>grep "closest('\[data-add-row\]')" services/app.js && grep "closest('\[data-mode\]')" services/app.js</automated>  
  </verify>
  <done>Sidebar and row listeners are consolidated into regional delegated handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Theme Swatches & Full Suite Verification</name>
  <files>services/app.js</files>
  <action>
    - Ensure the `.theme-picker` listener uses `event.target.closest('.theme-swatch')`.
    - Clean up `bindEvents` by removing all redundant querySelectorAll-based listeners.
    - Run the FULL Playwright test suite to ensure zero regressions in logic, interaction, or performance.
  </action>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>Event delegation is complete and full test suite passes (per User instruction).</done>
</task>

</tasks>

<verification>
- All buttons still function as expected.
- No regressions in adding rows or switching modes.
- Full test suite passes: npx playwright test.
</verification>

<success_criteria>
- Fragmented querySelectorAll listeners removed from app.js.
- Regional containers (#keypad, #sidebar, .left-panel, theme-picker) use delegation.
- `event.target.closest()` used for all delegated handlers.
- Full test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-library-event-optimization/03-03-SUMMARY.md`
</output>
