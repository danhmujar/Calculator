---
phase: 03-verification-parity-artifact-purge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [
    'ui/webgl/renderer.js',
    'ui/styles.css',
    'tests/phase-01.spec.js',
    'tests/phase-02.spec.js',
    'playwright.config.js',
  ]
autonomous: true
requirements: []
must_haves:
  truths:
    - 'Visual presentation matches original design without reliance on CSS blur fallbacks'
    - 'Application runs stably without legacy parity rendering overhead'
  artifacts:
    - path: 'ui/webgl/renderer.js'
      provides: 'Renderer logic without parity guards'
      min_lines: 500
    - path: 'ui/styles.css'
      provides: 'Purged CSS without backdrop-filter properties'
      min_lines: 2000
  key_links: []
---

<objective>
Purge dead code related to historical transitions, specifically `parity-*` classes and any remaining `backdrop-filter` CSS properties, and validate the end-state against requirements via the Playwright test suite.

Purpose: Finalize the migration to the WebGL underlay by eliminating legacy parity tools and pure-CSS fallbacks.
Output: Cleaned `ui/webgl/renderer.js`, completely purged CSS for `backdrop-filter`, and green Playwright tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-parity-artifact-purge/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge Parity Artifacts from Renderer</name>
  <files>ui/webgl/renderer.js</files>
  <read_first>ui/webgl/renderer.js</read_first>
  <action>
    Per D-01 (Delete parity hacks completely), remove the legacy `parity-webgl-only` and `parity-split-view` checks from the render loop guard in `ui/webgl/renderer.js`. 
    
    Target Purged Logic must be:
    ```javascript
    if (!document.body.classList.contains('webgl-active')) {
        this.context.clear([0, 0, 0, 0]);
        this.instanceCount = 0;
        return;
    }
    ```
    Ensure no other references to `parity-` remain in this file.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const txt = fs.readFileSync('ui/webgl/renderer.js', 'utf8'); process.exit(txt.includes('parity-webgl-only') ? 1 : 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `ui/webgl/renderer.js` does NOT contain the string `parity-webgl-only`
    - `ui/webgl/renderer.js` does NOT contain the string `parity-split-view`
  </acceptance_criteria>
  <done>Legacy parity checks removed from renderer per D-01</done>
</task>

<task type="auto">
  <name>Task 2: Configure Playwright and Validate Phase 1 Tests</name>
  <files>ui/styles.css, tests/phase-01.spec.js, playwright.config.js</files>
  <read_first>ui/styles.css, tests/phase-01.spec.js, playwright.config.js</read_first>
  <action>
    - [ ] Verify `ui/styles.css` is completely purged of `backdrop-filter` and `-webkit-backdrop-filter`. Remove them if any are found.
    - [ ] Per D-02 (Allow minor pixel differences), update `playwright.config.js` or the tests to configure `maxDiffPixelRatio` (e.g. `0.05` or appropriate) for visual comparisons.
    - [ ] Per D-03 (Standard Playwright traces), ensure Playwright is configured to output standard traces (`trace: 'on-first-retry'` or similar) in `playwright.config.js`.
    - [ ] Run the Playwright test `npx playwright test tests/phase-01.spec.js --reporter=line` (required by GEMINI.md).
    - [ ] If the test "REQ-WGL-02: CSS backdrop-filter is completely removed" fails (e.g., finding 316 offending elements), investigate the root cause. This may be due to how Playwright evaluates computed styles. If `getComputedStyle(el).backdropFilter` defaults to a string that passes the test's regex or truthy check (like `"none, none"` or `"rgba(0, 0, 0, 0)"`), update the test assertion in `tests/phase-01.spec.js` to correctly handle it (e.g., strictly checking that the style is `none` or empty string).

    Note: E2E Playwright test is strictly necessary here for visual regression and runs within the Nyquist limit (< 60s).

  </action>
  <verify>
    <automated>npx playwright test tests/phase-01.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `ui/styles.css` does NOT contain the string `backdrop-filter`
    - Playwright configured per D-02 and D-03
    - `npx playwright test tests/phase-01.spec.js --reporter=line` exits with 0
  </acceptance_criteria>
  <done>All legacy CSS fallbacks removed, Playwright configured, and Phase 1 test suite passes</done>
</task>

<task type="auto">
  <name>Task 3: Run Full Validation Suite</name>
  <files>tests/phase-02.spec.js</files>
  <read_first>tests/phase-02.spec.js</read_first>
  <action>
    Run the Phase 2 test suite using `npx playwright test tests/phase-02.spec.js --reporter=line` to confirm the WebGL underlay blur integration and UI synchronization remain perfectly functional after the artifact purge.
    Ensure that any visual regression assertions implicitly rely on the `maxDiffPixelRatio` configured per D-02 and use standard traces per D-03.

    Note: E2E Playwright test is strictly necessary here for visual regression and runs within the Nyquist limit (< 60s).

  </action>
  <verify>
    <automated>npx playwright test tests/phase-02.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/phase-02.spec.js --reporter=line` exits with 0
  </acceptance_criteria>
  <done>Full validation suite passes on the final integrated branch</done>
</task>

</tasks>

<verification>
- `parity-*` code is removed from `ui/webgl/renderer.js`
- `ui/styles.css` has no instances of `backdrop-filter`
- `npx playwright test` completes successfully for both phase 1 and 2 specs
</verification>

<success_criteria>
All Phase 3 requirements met: Parity scaffolding removed and automated test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-parity-artifact-purge/03-01-SUMMARY.md`
</output>
