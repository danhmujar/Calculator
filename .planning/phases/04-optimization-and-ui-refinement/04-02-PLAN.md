---
phase: 04-optimization-and-ui-refinement
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/styles.css
  - ui/eye-tracker.js
  - index.html
  - public/assets/sprites.svg
autonomous: true
requirements:
  - UI-REF-02
  - UI-REF-03
must_haves:
  truths:
    - Glassmorphic UI elements are sufficiently opaque for high contrast and readability
    - Chameleon eyes follow the cursor with smooth inertia and blink randomly
  artifacts:
    - path: ui/styles.css
      provides: Updated opacities and blink keyframes
      contains: color-mix(in srgb, var(--panel-bg) 85%, transparent)
    - path: ui/eye-tracker.js
      provides: EMA based smooth eye tracking loop
      contains: requestAnimationFrame
  key_links:
    - from: ui/eye-tracker.js
      to: ui/styles.css
      via: Eye tracking variables mapping to CSS transforms
---

<objective>
Increase the opacity of glass-morphic elements for a premium frosted look, and add life-like animations (inertia and blinking) to the chameleon eye tracker.

Purpose: Enhance the visual polish of the UI. Higher opacity increases legibility and contrast. Adding inertia and blinking to the eye tracker makes the character feel more organic and less robotic.
Output: Updated CSS variables for glass effects, an updated eye-tracker loop using EMA, and random blink animations via CSS keyframes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-optimization-and-ui-refinement/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase Glass Opacity for Legibility</name>
  <files>ui/styles.css</files>
  <read_first>
    - ui/styles.css
  </read_first>
  <action>
    Modify `ui/styles.css` to increase the opacity of glass-morphic elements (per D-03, D-04, D-05):
    1. Change light mode `--glass-bg` from `rgba(255, 255, 255, 0.55)` to `rgba(255, 255, 255, 0.82)`.
    2. Change light mode `--modal-glass-bg` from `rgba(255, 255, 255, 0.72)` to `rgba(255, 255, 255, 0.85)`.
    3. Change dark mode `--glass-bg` from `rgba(30, 32, 40, 0.65)` to `rgba(30, 32, 40, 0.82)`.
    4. Change dark mode `--modal-glass-bg` from `rgba(28, 30, 36, 0.82)` to `rgba(28, 30, 36, 0.85)`.
    5. Find all instances of `color-mix(in srgb, var(--panel-bg) 65%, transparent)` (or 80%) used for `.calc-card`, `.right-panel`, and `.calc-display` transparency and update the percentage to `85%`.
  </action>
  <acceptance_criteria>
    - `ui/styles.css` contains `--glass-bg: rgba(255, 255, 255, 0.82)`
    - `ui/styles.css` contains `--modal-glass-bg: rgba(28, 30, 36, 0.85)`
    - `ui/styles.css` contains `color-mix(in srgb, var(--panel-bg) 85%, transparent)`
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --reporter=line</automated>
  </verify>
  <done>Glass opacity variables have been successfully updated in ui/styles.css.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Eye Tracker (Inertia & Blinking)</name>
  <files>ui/eye-tracker.js, ui/styles.css, index.html, public/assets/sprites.svg</files>
  <read_first>
    - ui/eye-tracker.js
    - ui/styles.css
    - index.html
    - public/assets/sprites.svg
  </read_first>
  <action>
    1. Modify `ui/eye-tracker.js` to implement an Exponential Moving Average (EMA) loop. 
       - Instead of instantly updating variables in the `mousemove` event, update `targetX` and `targetY` coordinates.
       - Use a continuous `requestAnimationFrame` loop to lerp the current positions towards the targets (e.g., `currentX += (targetX - currentX) * 0.15`).
       - Apply the smoothed `currentX` and `currentY` coordinates back to the CSS custom properties within the `requestAnimationFrame` loop to effectively wire the tracking logic to the UI.
    2. Add random blink animations. In `ui/styles.css`, define a `@keyframes blink` animation that scales Y down to 0.1 and back to 1.
    3. Apply the blink animation to the `.eye` SVG group elements using a random delay or interval. Ensure `transform-origin` is set to `center` or appropriately so the eye scales from its middle.
    4. Update `index.html` and `public/assets/sprites.svg` if necessary to apply the CSS class or transform origins correctly so the SVG `<g class="eye">` collapses from its center properly.
  </action>
  <acceptance_criteria>
    - `ui/eye-tracker.js` contains a `requestAnimationFrame` loop that calculates EMA smoothing (e.g., updating current position iteratively).
    - `ui/styles.css` contains `@keyframes blink` with `transform: scaleY`.
    - The `.eye` elements in `index.html` or `ui/styles.css` have a `transform-origin` set.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --reporter=line</automated>
  </verify>
  <done>Eye tracking features EMA inertia and random blink animations.</done>
</task>

</tasks>

<verification>
Start the application and verify that the glass panels have a higher, frosted opacity. Move the mouse around and verify that the chameleon eyes follow with a slight, organic delay (inertia) rather than snapping instantly. Ensure the chameleon blinks occasionally.
</verification>

<success_criteria>
1. Glass UI elements use the updated `85%` mix ratio.
2. Eye tracking uses a `requestAnimationFrame` loop to smooth movement.
3. Chameleon eyes blink using CSS keyframes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-optimization-and-ui-refinement/04-02-SUMMARY.md`
</output>
