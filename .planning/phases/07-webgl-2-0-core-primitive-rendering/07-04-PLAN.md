---
phase: 07-webgl-2-0-core-primitive-rendering
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified: [ui/uimanager.js, ui/webgl/renderer.js]
autonomous: true
requirements: [REQ-WGL-03]

must_haves:
  truths:
    - "WebGL primitives correctly reflect the active CSS theme colors."
    - "Theme switching triggers an immediate WebGL re-render with updated colors."
  artifacts:
    - path: "ui/uimanager.js"
      provides: "CSS variable extraction and WebGL theme sync"
---

<objective>
Implement WebGL Theme Synchronization to ensure GPU-rendered primitives match the active DOM theme colors.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement Theme Color Extraction</name>
  <files>ui/uimanager.js</files>
  <action>
    Update `UIManager` to extract theme-specific colors from the document's computed styles.
    
    Implementation details:
    - Implement `getThemeColor(variableName)` using `getComputedStyle(document.body).getPropertyValue(variableName)`.
    - Create a `syncThemeColors()` method that retrieves primary and accent colors for the WebGL layer.
    - Cache these colors to avoid redundant DOM reads during the render loop.
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire Theme to WebGL Renderer</name>
  <files>ui/uimanager.js, ui/webgl/renderer.js</files>
  <action>
    Connect the extracted theme colors to the WebGL draw calls.
    
    Implementation details:
    - Update `WebGLRenderer.drawPrimitive` to accept a `color` parameter (already exists) or use a default theme color if none is provided.
    - In `UIManager.updateDisplay()`, ensure `webglRenderer.render()` is called with the latest theme context.
    - For verification, draw a test primitive (e.g., a glowing background for the active display) using the current theme's primary color.
  </action>
</task>

</tasks>

<verification>
1. Run `npm run dev`.
2. Change themes via the theme picker.
3. Confirm that the WebGL-rendered underlay elements (e.g., display glow or button highlights) change color in sync with the DOM theme.
</verification>

<output>
After completion, create `.planning/phases/07-webgl-2-0-core-primitive-rendering/07-04-SUMMARY.md`
</output>
