---
phase: 07-webgl-2-0-core-primitive-rendering
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [ui/webgl/buffers.js, ui/webgl/renderer.js, ui/uimanager.js]
autonomous: true
requirements: [REQ-WGL-03]

must_haves:
  truths:
    - "Dynamic buffers update without causing GPU stalls or flicker."
    - "The WebGL layer renders primitive backgrounds (buttons, cards) alongside the DOM UI."
    - "All draw calls use VAOs as per WebGL 2.0 best practices."
  artifacts:
    - path: "ui/webgl/buffers.js"
      provides: "VAO and VBO management"
    - path: "ui/webgl/renderer.js"
      provides: "Main rendering loop and draw call batching"
---

<objective>
Implement dynamic vertex buffer management and the main WebGL rendering loop to coordinate primitive rendering with the UIManager.

Purpose: Bridges the gap between GPU shaders and DOM-based application state.
Output: A functional rendering pipeline that updates and draws UI primitives in real-time.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-webgl-2-0-core-primitive-rendering/07-RESEARCH.md
@ui/webgl/context.js
@ui/webgl/shaders.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dynamic Buffer Manager</name>
  <files>ui/webgl/buffers.js</files>
  <action>
    Create a `BufferManager` class to manage vertex data for dynamic UI components using VAOs.
    
    Implementation details:
    - Implement `createVAO(gl, data)` that returns a `WebGLVertexArrayObject` pre-configured with `vertexAttribPointer`.
    - Implement **Buffer Orphaning** logic for updates: call `gl.bufferData(target, size, usage)` with `null` before sending new data.
    - Use `gl.STATIC_DRAW` for UI boundaries and `gl.DYNAMIC_DRAW` for row/button geometry.
    - Support interleaved vertex data (Position + TexCoord).
  </action>
  <verify>
    <automated>Verify no WebGL warnings in console during multiple buffer updates.</automated>
  </verify>
  <done>Buffer manager successfully handles VAO creation and orphaning.</done>
</task>

<task type="auto">
  <name>Task 2: Implement WebGL Renderer & Loop Integration</name>
  <files>ui/webgl/renderer.js, ui/uimanager.js</files>
  <action>
    Create the `WebGLRenderer` and integrate it into the existing application rendering cycle.
    
    Implementation details:
    - `ui/webgl/renderer.js`: Implement `render()` that binds programs/VAOs and performs draw calls.
    - Implement `drawPrimitive(rect, color, radius)` as an entry point for primitive rendering.
    - Wire `ui/uimanager.js`'s `updateDisplay()` or `setupResizeHandler()` to call `webglRenderer.render()`.
    - Integration: Ensure the WebGL layer correctly clears and draws its contents at 60 FPS.
  </action>
  <verify>
    <automated>Run `npm run dev` and confirm that primitive backgrounds are visible behind the DOM calculator buttons.</automated>
  </verify>
  <done>Application loop now includes WebGL-accelerated primitive rendering.</done>
</task>

</tasks>

<verification>
1. Manually verify that WebGL-rendered primitives align perfectly with DOM elements.
2. Confirm performance is stable (60 FPS) when adding or removing scientific rows.
</verification>

<success_criteria>
- WebGL 2.0 VAOs are used for all draw calls.
- Buffer orphaning prevents pipeline stalls during layout changes.
- Primitive shapes render consistently across different device pixel ratios.
</success_criteria>

<output>
After completion, create `.planning/phases/07-webgl-2-0-core-primitive-rendering/07-03-SUMMARY.md`
</output>
