---
phase: 07-webgl-2-0-core-primitive-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ui/webgl/context.js, ui/uimanager.js, tests/renderer.spec.js]
autonomous: true
requirements: [REQ-WGL-01]

must_haves:
  truths:
    - "A WebGL 2.0 canvas is injected into the DOM as the first child of .layout-container."
    - "The canvas covers the full viewport and has z-index: -1."
    - "The canvas resizes synchronously with the window without layout lag."
  artifacts:
    - path: "ui/webgl/context.js"
      provides: "WebGL 2.0 context management"
    - path: "tests/renderer.spec.js"
      provides: "Automated verification of WebGL initialization"
  key_links:
    - from: "ui/uimanager.js"
      to: "ui/webgl/context.js"
      via: "instantiation in init()"
---

<objective>
Initialize the WebGL 2.0 context and integrate the rendering layer into the application lifecycle using the Underlay Pattern.

Purpose: Establishes the GPU-accelerated foundation for all future UI rendering while maintaining DOM-based interactivity and accessibility.
Output: A persistent, responsive WebGL 2.0 rendering context accessible via the UIManager.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-webgl-2-0-core-primitive-rendering/07-RESEARCH.md
@ui/uimanager.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WebGL 2.0 Context Manager</name>
  <files>ui/webgl/context.js, tests/renderer.spec.js</files>
  <action>
    Create a `WebGLContext` class in `ui/webgl/context.js` to manage the lifecycle of the WebGL layer.
    
    Implementation details:
    - Constructor should create a `<canvas>` element with ID `webgl-underlay`.
    - Initialize WebGL 2.0 context with attributes: `{ alpha: false, antialias: true, depth: false, stencil: false }`.
    - Implement `resize()` method that updates `canvas.width/height` and `gl.viewport` based on `window.innerWidth/innerHeight * window.devicePixelRatio`.
    - Implement `clear(color)` method for verification (default to background color).
    - Handle `webglcontextlost` and `webglcontextrestored` events with console logging.
    
    Create `tests/renderer.spec.js` using Playwright to verify:
    - The canvas exists and has a valid `webgl2` context.
    - `gl.getParameter(gl.VERSION)` returns a string containing "WebGL 2.0".
  </action>
  <verify>
    <automated>npx playwright test tests/renderer.spec.js</automated>
  </verify>
  <done>WebGLContext initialized and verified via automated test.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate WebGL Underlay into UIManager</name>
  <files>ui/uimanager.js</files>
  <action>
    Integrate `WebGLContext` into the existing `UIManager` to ensure synchronized lifecycle management.
    
    Implementation details:
    - Import `WebGLContext` from `./webgl/context.js`.
    - In `UIManager.init()`, instantiate `this.webgl = new WebGLContext()`.
    - Inject `this.webgl.canvas` as the first child of `.layout-container` to ensure it stays behind all other UI elements.
    - Apply CSS styles to the canvas via `ui/webgl/context.js`: `position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; z-index: -1; pointer-events: none;`.
    - Call `this.webgl.clear([r, g, b, a])` in `init()` to verify visibility.
  </action>
  <verify>
    <automated>npm run dev (Manual check: Inspect DOM for #webgl-underlay as first child of .layout-container)</automated>
  </verify>
  <done>WebGL canvas is present in DOM and correctly positioned behind the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronous Resize Handling</name>
  <files>ui/uimanager.js</files>
  <action>
    Ensure the WebGL layer resizes in perfect sync with the DOM UI to prevent jitter.
    
    Implementation details:
    - Modify `UIManager.setupResizeHandler()` to call `this.webgl.resize()` inside the `window.resize` listener.
    - Ensure `resize()` is called within a `requestAnimationFrame` if necessary, or directly if the DOM layout is stable.
    - Verify that no scrollbars are introduced by the full-viewport canvas.
  </action>
  <verify>
    <automated>Check console for GL viewport updates during window resize in browser.</automated>
  </verify>
  <done>WebGL viewport updates correctly on window resize without visual artifacts.</done>
</task>

</tasks>

<verification>
1. Run `npx playwright test tests/renderer.spec.js` to confirm WebGL 2.0 context is valid.
2. Run `npm run dev`, open DevTools, and verify `#webgl-underlay` is the first child of `.layout-container`.
3. Resize window and ensure canvas dimensions match viewport.
</verification>

<success_criteria>
- WebGL 2.0 context is successfully initialized on the main thread.
- Rendering layer is positioned behind the DOM UI (Underlay pattern).
- Zero visual regressions to existing DOM UI.
- Automated tests pass for context verification.
</success_criteria>

<output>
After completion, create `.planning/phases/07-webgl-2-0-core-primitive-rendering/07-01-SUMMARY.md`
</output>
