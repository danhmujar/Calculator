---
phase: 07-webgl-2-0-core-primitive-rendering
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [ui/webgl/shaders.js, ui/webgl/shaders/primitive.vert, ui/webgl/shaders/primitive.frag]
autonomous: true
requirements: [REQ-WGL-02]

must_haves:
  truths:
    - "Shaders compile and link successfully without errors."
    - "SDF-based primitives (rounded rectangles) are rendered with smooth anti-aliased edges."
    - "Shader uniforms (color, resolution, radius) can be updated in real-time."
  artifacts:
    - path: "ui/webgl/shaders.js"
      provides: "GLSL compilation engine"
    - path: "ui/webgl/shaders/primitive.frag"
      provides: "High-precision SDF primitive rendering"
---

<objective>
Establish the shader compilation pipeline and implement high-precision Signed Distance Function (SDF) shaders for rendering UI primitives.

Purpose: Offloads shape rendering to the GPU with infinite scaling and perfect anti-aliasing.
Output: A library of GLSL shaders and a manager to handle compilation and uniform updates.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-webgl-2-0-core-primitive-rendering/07-RESEARCH.md
@ui/webgl/context.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Shader Compilation Manager</name>
  <files>ui/webgl/shaders.js</files>
  <action>
    Create a `ShaderManager` class in `ui/webgl/shaders.js` to automate GLSL 3.00 ES compilation.
    
    Implementation details:
    - Implement `compile(gl, type, source)` that returns a `WebGLShader` and handles compilation error logging (including line numbers).
    - Implement `createProgram(gl, vertSource, fragSource)` that links shaders and handles linking errors.
    - Support template literal shader sources for zero-dependency loading in this phase.
    - Implement a `setUniforms(gl, program, uniforms)` helper for bulk uniform updates.
  </action>
  <verify>
    <automated>Verify console shows "Shader program linked successfully" on initialization.</automated>
  </verify>
  <done>Shader compilation pipeline established with robust error reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SDF Primitive Shaders</name>
  <files>ui/webgl/shaders/primitive.vert, ui/webgl/shaders/primitive.frag</files>
  <action>
    Develop vertex and fragment shaders for high-precision UI primitives based on research SDFs.
    
    Implementation details:
    - **Vertex Shader (`primitive.vert`)**: Handle clip-space coordinate transformation and pass texture coordinates [0, 1] to fragment shader.
    - **Fragment Shader (`primitive.frag`)**: Implement the `sdRoundedBox` function from RESEARCH.md.
    - Include anti-aliasing using `fwidth()` and `smoothstep()`.
    - Support uniforms: `u_resolution`, `u_rectSize`, `u_radius`, `u_color`, `u_offset`.
  </action>
  <verify>
    <automated>Manually verify smooth edges of a rendered test rectangle (e.g., clear color vs primitive color).</automated>
  </verify>
  <done>SDF shaders created and capable of rendering anti-aliased rounded rectangles.</done>
</task>

</tasks>

<verification>
1. Check browser console for shader compilation errors.
2. Confirm that basic uniforms (resolution) are correctly passed to the GPU.
</verification>

<success_criteria>
- ShaderManager successfully compiles GLSL 3.00 ES code.
- SDF-based rounded rectangles render with no pixelation on 1x or 2x DPI displays.
- Fragment shader correctly handles dynamic corner radii.
</success_criteria>

<output>
After completion, create `.planning/phases/07-webgl-2-0-core-primitive-rendering/07-02-SUMMARY.md`
</output>
